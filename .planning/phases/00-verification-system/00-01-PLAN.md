---
phase: 00-verification-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "package.json"
]
autonomous: true
user_setup: [
  {
    service: "twilio",
    why: "SMS verification requires Twilio SDK",
    env_vars: [
      {
        name: "TWILIO_SID",
        source: "Twilio Dashboard -> Console -> Account SID"
      },
      {
        name: "TWILIO_TOKEN",
        source: "Twilio Dashboard -> Console -> Auth Token"
      },
      {
        name: "TWILIO_WHATSAPP_FROM",
        source: "Twilio Dashboard -> Messaging -> Try it out -> Send a WhatsApp message (sandbox number)"
      }
    ]
  }
]
must_haves:
  truths:
    - "SMS verification sends codes successfully via Twilio"
    - "Email verification sends codes successfully via Mailgun"
    - "Backend verification endpoints respond correctly to API requests"
  artifacts:
    - path: "package.json"
      provides: "Twilio dependency declaration"
      contains: "twilio"
    - path: "server/services/verificationService.ts"
      provides: "Email and SMS verification service"
      exports: ["generateVerificationCode", "validateVerificationCode"]
    - path: "server/api/verification/generate.post.ts"
      provides: "Verification code generation API endpoint"
      exports: ["default"]
    - path: "server/api/verification/validate.post.ts"
      provides: "Verification code validation API endpoint"
      exports: ["default"]
  key_links:
    - from: "server/api/verification/generate.post.ts"
      to: "server/services/verificationService.ts"
      via: "import { generateVerificationCode }"
      pattern: "generateVerificationCode\\(\\{.*obraId.*email.*phone.*method.*\\}\\)"
    - from: "server/api/verification/validate.post.ts"
      to: "server/services/verificationService.ts"
      via: "import { validateVerificationCode }"
      pattern: "validateVerificationCode\\(code.*obraId\\)"
    - from: "server/services/verificationService.ts"
      to: "twilio"
      via: "require('twilio')"
      pattern: "require\\('twilio'\\)"
---

<objective>
**Objective:** Install Twilio package and verify backend verification system is operational

**Purpose:** The verification backend is 90% complete but the Twilio package is not installed. This plan installs the dependency and verifies that the existing verification service works correctly.

**Output:** Working verification system with both email (Mailgun) and SMS (Twilio) functionality ready for frontend integration.
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-verification-system/00-RESEARCH.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Twilio Package</name>
  <files>package.json</files>
  <action>
    Run `bun add twilio` to install the Twilio Node.js SDK. This is required for SMS verification functionality.

    The existing verificationService.ts uses `require('twilio')` but the package is not in package.json. After installation, verify the package appears in dependencies.

    DO NOT modify any service code - the verificationService.ts is already complete.
  </action>
  <verify>test -f node_modules/twilio/package.json || grep '"twilio"' package.json</verify>
  <done>Twilio package appears in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Verify Backend Verification Endpoints</name>
  <files>server/services/verificationService.ts, server/api/verification/generate.post.ts, server/api/verification/validate.post.ts</files>
  <action>
    Read and verify the following files exist and are correctly implemented:
    - `server/services/verificationService.ts` - Should have generateVerificationCode and validateVerificationCode functions
    - `server/api/verification/generate.post.ts` - Should handle POST requests with obraId, email, phone, method
    - `server/api/verification/validate.post.ts` - Should handle POST requests with code, obraId
    - `server/models/VerificationCode.ts` - Should have VerificationCode model with TTL index
    - `server/schemas/verification.ts` - Should have VerificationCodeCreateSchema and VerificationCodeValidateSchema
    - `server/utils/rateLimit.ts` - Should have rateLimitVerification and rateLimitValidate middleware

    DO NOT modify any files - just verify they exist and are complete.
  </action>
  <verify>test -f server/services/verificationService.ts && test -f server/api/verification/generate.post.ts && test -f server/api/verification/validate.post.ts && test -f server/models/VerificationCode.ts && test -f server/schemas/verification.ts && test -f server/utils/rateLimit.ts</verify>
  <done>All verification backend files are present and correctly structured</done>
</task>

<task type="auto">
  <name>Task 3: Verify Environment Configuration</name>
  <files>nuxt.config.ts</files>
  <action>
    Check that the following environment variables are referenced in nuxt.config.ts:
    - `mailgun.apiKey` (from API_KEY_MAILGUN)
    - `mailgun.domain` (from MG_DOMAIN)
    - `twilioSid` (from TWILIO_SID)
    - `twilioToken` (from TWILIO_TOKEN)
    - `twilioWhatsAppFrom` (from TWILIO_WHATSAPP_FROM)

    Log which environment variables are configured. If any are missing from the config, note them in the summary.

    DO NOT add new environment variables - just verify existing configuration.
  </action>
  <verify>grep -E "(twilio|mailgun)" nuxt.config.ts</verify>
  <done>All verification-related environment variables are referenced in nuxt.config.ts</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Twilio package is installed in node_modules and listed in package.json
2. All verification backend files exist (verificationService.ts, generate.post.ts, validate.post.ts)
3. Environment configuration references Mailgun and Twilio variables
4. No changes to existing service files (they were already complete)
</verification>

<success_criteria>
1. `bun add twilio` completes successfully
2. package.json contains "twilio" dependency
3. All backend verification files are verified to exist and be complete
4. Environment variables are correctly configured in nuxt.config.ts
5. Ready to proceed with frontend integration (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/00-verification-system/00-01-SUMMARY.md` with:
- Twilio installation status
- Backend verification files verified
- Environment configuration status
- Any missing environment variables noted
- Ready for frontend integration confirmation
</output>
