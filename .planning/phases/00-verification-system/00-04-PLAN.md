---
phase: 00-verification-system
plan: 04
type: execute
wave: 4
depends_on: ["00-03"]
files_modified: [
  ".planning/phases/00-verification-system/00-TEST-REPORT.md"
]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Email verification sends code via Mailgun API"
    - "SMS verification sends code via Twilio API"
    - "Code validation accepts valid codes and rejects invalid/expired codes"
    - "Resend countdown prevents spam (60 seconds)"
    - "Optional verification allows form submission without verification"
    - "All error messages are in Spanish"
  artifacts:
    - path: ".planning/phases/00-verification-system/00-TEST-REPORT.md"
      provides: "Test report documenting verification system functionality"
      min_lines: 50
  key_links:
    - from: ".planning/phases/00-verification-system/00-TEST-REPORT.md"
      to: "app/composables/useVerification.ts"
      via: "Composable functional integration test"
      pattern: "sendCode.*validateCode.*resendCountdown"
    - from: ".planning/phases/00-verification-system/00-TEST-REPORT.md"
      to: "app/components/VerificationForm.vue"
      via: "Component functional integration test"
      pattern: "handleSendCode.*handleValidate.*emit.*verified"
    - from: ".planning/phases/00-verification-system/00-TEST-REPORT.md"
      to: "server/api/verification/generate.post.ts"
      via: "API endpoint functional test"
      pattern: "POST.*verification/generate.*200"
---

<objective>
**Objective:** Test verification system and document functionality

**Purpose:** Verify that the complete verification system (backend + frontend) works correctly for both email and SMS methods, and document the results.

**Output:** Comprehensive test report documenting all verification flows, error cases, and Spanish localization.
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-verification-system/00-RESEARCH.md
@.planning/phases/00-verification-system/00-03-SUMMARY.md
@server/services/verificationService.ts (backend verification logic)
@app/composables/useVerification.ts (frontend verification composable)
@app/components/VerificationForm.vue (verification UI component)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete verification system with email/SMS support</what-built>
  <how-to-verify>
    1. Start dev server: `bun dev`
    2. Navigate to a public issue reporting page (QR code access)
    3. Fill out Steps 1-4 of the form
    4. On Step 5 (Verification), test the following:

    **Email Verification Test:**
    - Select "Email" as verification method
    - Click "Enviar código de verificación"
    - Check your email for the 6-digit code
    - Enter the code in the verification field
    - Click "Verificar código"
    - Verify success message appears

    **SMS Verification Test:**
    - Select "SMS" as verification method
    - Click "Enviar código de verificación"
    - Check your phone for the SMS code
    - Enter the code in the verification field
    - Click "Verificar código"
    - Verify success message appears

    **Resend Countdown Test:**
    - After sending a code, verify "Reenviar" button is disabled
    - Wait for countdown to reach 0
    - Verify "Reenviar código" button becomes enabled
    - Click resend and verify new code is sent

    **Optional Verification Test:**
    - Skip verification entirely
    - Click "Enviar reporte" button
    - Verify form submits without verification

    **Error Cases Test:**
    - Enter invalid code (wrong digits)
    - Verify error message in Spanish appears
    - Try to resend before countdown expires
    - Verify button is disabled with countdown shown

    5. Check browser console for errors
    6. Verify all text is in Spanish
    7. Check Network tab for API calls to /api/verification/generate and /api/verification/validate
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create Test Report Documentation</name>
  <files>.planning/phases/00-verification-system/00-TEST-REPORT.md</files>
  <action>
    Create a test report at `.planning/phases/00-verification-system/00-TEST-REPORT.md` documenting the verification system testing results.

    Include the following sections:
    ```markdown
    # Verification System Test Report

    **Date:** [Current date]
    **Phase:** 00-verification-system
    **Plans:** 00-01, 00-02, 00-03, 00-04

    ## Summary

    [Brief summary of testing results]

    ## Test Environment

    - Dev server running on: [URL]
    - Test date: [Date/time]
    - Browser: [Browser used for testing]

    ## Test Results

    ### Backend Verification (Plan 00-01)

    - [ ] Twilio package installed
    - [ ] Verification service endpoints functional
    - [ ] Environment variables configured
    - [ ] Rate limiting working

    ### Composable Testing (Plan 00-02)

    - [ ] useVerification composable loads
    - [ ] sendCode() calls /api/verification/generate
    - [ ] validateCode() calls /api/verification/validate
    - [ ] Resend countdown works (60 seconds)
    - [ ] Error handling in Spanish

    ### Component Testing (Plan 00-03)

    - [ ] VerificationForm component renders
    - [ ] Email/SMS method selection works
    - [ ] Send code button triggers API call
    - [ ] Code input accepts 6 digits
    - [ ] Verify button validates code
    - [ ] Resend button with countdown works
    - [ ] Verified status displays correctly
    - [ ] Spanish text throughout

    ### Integration Testing (Plan 00-04)

    - [ ] IssueReportForm Step 5 shows VerificationForm
    - [ ] Optional verification allows skip
    - [ ] Form submits without verification
    - [ ] Form submits with verification
    - [ ] No existing functionality broken

    ## Test Cases

    ### Case 1: Email Verification Flow
    **Steps:**
    1. Select Email method
    2. Click "Enviar código"
    3. Receive email with code
    4. Enter code
    5. Click "Verificar código"

    **Expected:** Success message, verified status true
    **Actual:** [Result]

    ### Case 2: SMS Verification Flow
    **Steps:**
    1. Select SMS method
    2. Click "Enviar código"
    3. Receive SMS with code
    4. Enter code
    5. Click "Verificar código"

    **Expected:** Success message, verified status true
    **Actual:** [Result]

    ### Case 3: Invalid Code
    **Steps:**
    1. Send verification code
    2. Enter invalid 6-digit code
    3. Click "Verificar código"

    **Expected:** Error message in Spanish
    **Actual:** [Result]

    ### Case 4: Resend Countdown
    **Steps:**
    1. Send verification code
    2. Check resend button status
    3. Wait 60 seconds
    4. Click resend

    **Expected:** Button disabled during countdown, enabled after
    **Actual:** [Result]

    ### Case 5: Optional Verification
    **Steps:**
    1. Skip verification
    2. Click "Enviar reporte"

    **Expected:** Form submits successfully
    **Actual:** [Result]

    ## Issues Found

    [List any issues found during testing]

    ## Spanish Localization

    - [ ] All UI text in Spanish
    - [ ] All error messages in Spanish
    - [ ] Email template in Spanish
    - [ ] SMS message in Spanish

    ## Conclusion

    [Overall assessment of verification system]

    ## Next Steps

    [Any follow-up work needed]
    ```

    Fill in the test results based on actual testing performed in Task 1.

    Key requirements:
    - Document all test cases and results
    - Note any issues found
    - Verify Spanish localization throughout
    - Check all API endpoints are called correctly
    - Confirm optional verification works as expected
  </action>
  <verify>Test report created with all test cases documented</verify>
  <done>Test report documents verification system functionality and test results</done>
</task>

<task type="auto">
  <name>Task 2.5: Verify Functional Integration Between Components</name>
  <files>app/components/VerificationForm.vue, app/composables/useVerification.ts, server/api/verification/generate.post.ts</files>
  <action>
    Verify the actual functional integration between components, not just string presence:

    1. **Composable to API Integration:**
       - Verify useVerification.sendCode() properly calls `/api/verification/generate`
       - Verify response structure matches: `{ success: true, data: { message, method, expiresAt } }`
       - Verify useVerification.validateCode() properly calls `/api/verification/validate`
       - Check that errors are caught and returned with Spanish messages

    2. **Component to Composable Integration:**
       - Verify VerificationForm imports useVerification correctly
       - Verify handleSendCode constructs proper VerificationOptions
       - Verify handleValidate passes correct parameters (code, obraId)
       - Verify component emits 'verified' event on successful validation

    3. **Form to Component Integration:**
       - Verify IssueReportForm passes obraId, email, phone props to VerificationForm
       - Verify handleVerificationComplete receives and handles verified status
       - Verify canSubmit computed does NOT block submission (verification is optional)

    4. **Response Flow Verification:**
       - API returns: `{ success: true, data: { message: "...", method: "email", expiresAt: "..." } }`
       - Composable returns: response directly (no wrapping)
       - Component accesses: result.success, result.data.expiresAt
       - All error handling uses Spanish messages

    This verification goes beyond string presence to ensure actual functional connectivity.
  </action>
  <verify>grep -q "useVerification" app/components/VerificationForm.vue && grep -q "sendCode.*obraId" app/components/VerificationForm.vue && grep -q "canProceed" app/components/IssueReportForm.vue</verify>
  <done>Functional integration verified between all layers of the verification system</done>
</task>

<task type="auto">
  <name>Task 3: Verify Spanish Localization Complete</name>
  <files>app/components/VerificationForm.vue, app/composables/useVerification.ts, server/services/verificationService.ts</files>
  <action>
    Review all verification-related files to ensure complete Spanish localization:

    1. Check VerificationForm.vue:
       - All UI labels and text in Spanish
       - Error messages in Spanish
       - Button text in Spanish
       - Help text in Spanish

    2. Check useVerification.ts:
       - Error messages in Spanish
       - No English error strings

    3. Check verificationService.ts:
       - Email template in Spanish
       - SMS message in Spanish
       - Error responses in Spanish

    4. Check verification API endpoints:
       - Error messages in Spanish
       - Success messages in Spanish

    If any English text is found, note it in the test report. This is for documentation purposes only - DO NOT modify files.

    Key requirements:
    - Review all verification files for Spanish localization
    - Document any English text found
    - Note which files need updates if any
  </action>
  <verify>All verification files reviewed for Spanish localization</verify>
  <done>Spanish localization verified and documented in test report</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Test report created at .planning/phases/00-verification-system/00-TEST-REPORT.md
2. All test cases documented with results
3. Spanish localization verified across all files
4. Issues (if any) documented in test report
5. Overall assessment included
6. Next steps documented if needed
</verification>

<success_criteria>
1. Complete verification system tested end-to-end
2. Email verification flow working
3. SMS verification flow working (if Twilio configured)
4. Resend countdown working correctly
5. Optional verification confirmed working
6. Spanish localization verified throughout
7. Test report documents all findings
8. Ready for production deployment (if tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/00-verification-system/00-04-SUMMARY.md` with:
- Testing summary
- All test results
- Issues found (if any)
- Spanish localization status
- Production readiness assessment
- Recommendations for deployment
- Phase completion confirmation
</output>
