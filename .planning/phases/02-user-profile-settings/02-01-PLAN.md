---
phase: 02-user-profile-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/protected/settings/perfil.vue
  - app/pages/protected/settings/index.vue
autonomous: false
must_haves:
  truths:
    - "Users can view their profile information (nombre, email, telefono)"
    - "Users can edit their profile information and save changes"
    - "Profile updates persist across page refreshes"
    - "Form validation prevents invalid email or phone formats"
    - "Spanish error messages display for validation and API errors"
  artifacts:
    - path: "app/pages/protected/settings/perfil.vue"
      provides: "User profile view/edit form with Nuxt UI v4 components"
      min_lines: 80
    - path: "app/pages/protected/settings/index.vue"
      provides: "Updated tabs configuration with perfil tab"
      contains: "to: '/protected/settings/perfil'"
  key_links:
    - from: "app/pages/protected/settings/perfil.vue"
      to: "app/stores/user.ts"
      via: "useUserStore().updateUser()"
      pattern: "userStore\\.updateUser\\("
    - from: "app/pages/protected/settings/perfil.vue"
      to: "/api/auth/me"
      via: "PUT request"
      pattern: "\\$fetch.*\\/api\\/auth\\/me.*method.*PUT"
---

<objective>
Create the Perfil (Profile) tab in settings with user profile view/edit form

Purpose: Allow users to view and edit their profile information (name, email, phone, company details) using existing backend API
Output: Working perfil.vue page that displays user data and saves changes via useUserStore.updateUser()
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-user-profile-settings/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/pages/protected/settings/index.vue
@app/pages/protected/settings/qr.vue
@app/stores/user.ts
@server/api/auth/me.put.ts
@server/models/User.ts
@app/components/qr/QRConfigForm.vue
</context>

<tasks>

<task type="auto">
  <name>Create perfil.vue page with profile form</name>
  <files>app/pages/protected/settings/perfil.vue</files>
  <action>
    Create perfil.vue page following the qr.vue pattern:

    1. Page structure:
       - Page header with title "Perfil de Usuario" and description
       - UCard component containing the profile form
       - Loading and error states matching qr.vue pattern

    2. Form fields (using UInput with appropriate icons):
       - Nombre completo (name)
       - Email (email) - required, type="email"
       - Teléfono (matriz_tel) - optional, type="tel"
       - Nombre de empresa (matriz_nombre) - optional
       - CIF (matriz_cif) - optional
       - Dirección (matriz_dir) - optional
       - Población (matriz_pob) - optional
       - Código Postal (matriz_cp) - optional
       - Observaciones (matriz_obs) - optional, use UTextarea

    3. Form behavior:
       - Load current user data from userStore.user on mount
       - formData ref with all profile fields
       - hasChanges computed to track modifications
       - Save button calls userStore.updateUser() with changed fields
       - Show green toast on success: "Perfil actualizado correctamente"
       - Show red toast on error with API error message
       - Reset button restores original values from userStore

    4. Nuxt UI v4 components:
       - UCard for form container
       - UInput for text/email/phone fields
       - UTextarea for observaciones
       - UButton for save/reset actions
       - UIcon for field icons
       - UAlert for info banner

    5. Spanish labels and helper text throughout
  </action>
  <verify>
    1. File exists at app/pages/protected/settings/perfil.vue
    2. Component uses Nuxt UI v4 components (UCard, UInput, UButton)
    3. Uses useUserStore for profile updates
    4. Has loading, error, and form states
  </verify>
  <done>
    - perfil.vue page exists with complete profile form
    - Form loads user data on mount
    - Save button triggers updateUser() with form data
    - Reset button restores original values
    - Toast notifications for success/error states
  </done>
</task>

<task type="auto">
  <name>Update settings index.vue with perfil tab</name>
  <files>app/pages/protected/settings/index.vue</files>
  <action>
    Update the tabs array in app/pages/protected/settings/index.vue:

    1. Add perfil tab entry to tabs array:
       - label: 'Perfil'
       - icon: 'i-heroicons-user'
       - value: 'profile'
       - to: '/protected/settings/perfil'
       - description: 'Información de tu perfil'

    2. Update currentTab computed to include perfil detection:
       - Add check for path.includes('/settings/perfil') returning 'profile'

    3. Verify tab order matches requirements: General, Códigos QR, Perfil

    The tab structure should already be present from Phase 2 requirements - just verify it's correct.
  </action>
  <verify>
    1. Tabs array includes perfil entry with correct properties
    2. currentTab computed handles /settings/perfil route
    3. Tab navigation works correctly
  </verify>
  <done>
    - Perfil tab appears in settings navigation
    - Clicking Perfil tab navigates to /protected/settings/perfil
    - Tab is highlighted correctly when on perfil page
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete perfil.vue page with user profile form integrated into settings navigation</what-built>
  <how-to-verify>
    1. Start dev server: bun dev
    2. Navigate to http://localhost:3000/protected/settings
    3. Click on "Perfil" tab
    4. Verify:
       - Page loads without errors
       - User profile data is pre-filled in form fields
       - All form fields render correctly with icons
       - "Guardar cambios" button is disabled when no changes
       - Modify a field (e.g., teléfono) and verify button enables
       - Click "Guardar cambios" and verify success toast appears
       - Refresh page and verify changes persist
       - Click "Restablecer" and verify form reverts to saved values
    5. Test error handling:
       - Try invalid email format
       - Try duplicate email (if testing with another account)
  </how-to-verify>
  <resume-signal>Type "approved" if perfil form works correctly, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Navigate to /protected/settings/perfil - page loads without console errors
2. User profile data displays correctly in form fields
3. Form validation works for email format
4. Save button updates profile and shows success toast
5. Changes persist after page refresh
6. Reset button restores original values
7. Spanish labels throughout
8. Nuxt UI v4 components render correctly
9. Tab navigation works from settings index
</verification>

<success_criteria>
1. Users can view their complete profile information in the form
2. Users can edit profile fields and save changes successfully
3. Profile updates persist across sessions (database confirmed)
4. Form validation prevents invalid data submission
5. Error handling with Spanish messages for API failures
6. Tab navigation properly integrates perfil page into settings
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-profile-settings/02-01-SUMMARY.md`
</output>
