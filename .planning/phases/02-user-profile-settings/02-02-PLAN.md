---
phase: 02-user-profile-settings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/protected/settings/contrasena.vue
  - app/pages/protected/settings/index.vue
autonomous: false
must_haves:
  truths:
    - "Users can change their password with current password verification"
    - "Password change requires current password confirmation"
    - "New password must be confirmed (matching entry)"
    - "Password change shows success/error feedback"
    - "OAuth users without passwords see appropriate message"
  artifacts:
    - path: "app/pages/protected/settings/contrasena.vue"
      provides: "Password change form with validation"
      min_lines: 80
    - path: "app/pages/protected/settings/index.vue"
      provides: "Updated tabs configuration with contrasena tab"
      contains: "to: '/protected/settings/contrasena'"
  key_links:
    - from: "app/pages/protected/settings/contrasena.vue"
      to: "app/stores/user.ts"
      via: "useUserStore().changePassword()"
      pattern: "userStore\\.changePassword\\("
    - from: "app/pages/protected/settings/contrasena.vue"
      to: "/api/auth/change-password"
      via: "POST request"
      pattern: "\\$fetch.*\\/api\\/auth\\/change-password.*method.*POST"
---

<objective>
Create the Contraseña (Password) tab in settings with secure password change form

Purpose: Allow users to change their password with current password verification and confirmation
Output: Working contrasena.vue page that validates current password and updates to new password
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-user-profile-settings/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/pages/protected/settings/index.vue
@app/pages/protected/settings/qr.vue
@app/stores/user.ts
@server/api/auth/change-password.post.ts
@server/models/User.ts
</context>

<tasks>

<task type="auto">
  <name>Create contrasena.vue page with password change form</name>
  <files>app/pages/protected/settings/contrasena.vue</files>
  <action>
    Create contrasena.vue page following the qr.vue pattern:

    1. Page structure:
       - Page header with title "Cambiar Contraseña" and description
       - UCard component containing the password form
       - Loading and error states matching qr.vue pattern

    2. Form fields (using UInput with type="password" and show/hide toggle):
       - Contraseña actual (currentPassword) - required
       - Nueva contraseña (newPassword) - required, min 8 characters
       - Confirmar nueva contraseña (confirmPassword) - required, must match newPassword

    3. Form validation:
       - All fields required
       - newPassword minimum 8 characters
       - confirmPassword must match newPassword (client-side validation before API call)
       - Show inline error for mismatched passwords

    4. Form behavior:
       - formData ref with currentPassword, newPassword, confirmPassword
       - isValid computed for form validation
       - Save button calls userStore.changePassword(currentPassword, newPassword)
       - On success: show green toast "Contraseña cambiada correctamente", clear all form fields
       - On error: show red toast with error message (likely "La contraseña actual es incorrecta")
       - Loading state during API call

    5. Security features:
       - Password fields use type="password"
       - No password display in error messages
       - Form clears after successful change

    6. OAuth users handling:
       - Check if user has password (userStore.user.password exists or detect OAuth via googleId/githubId)
       - Show message if OAuth user: "Tu cuenta está vinculada a Google/GitHub y no tiene contraseña. Para cambiar tu contraseña, primero debes restablecerla mediante 'Olvidé mi contraseña'."

    7. Nuxt UI v4 components:
       - UCard for form container
       - UInput for password fields
       - UButton for save action
       - UIcon for field icons
       - UAlert for info/warning banners

    8. Spanish labels and helper text throughout
  </action>
  <verify>
    1. File exists at app/pages/protected/settings/contrasena.vue
    2. Component uses Nuxt UI v4 components (UCard, UInput, UButton)
    3. Uses useUserStore.changePassword() for password updates
    4. Has password confirmation validation
    5. Has OAuth user detection
  </verify>
  <done>
    - contrasena.vue page exists with complete password form
    - Current password verification required
    - New password confirmation required
    - Client-side validation for password match
    - Success clears form and shows toast
    - OAuth users see appropriate message
  </done>
</task>

<task type="auto">
  <name>Update settings index.vue with contrasena tab</name>
  <files>app/pages/protected/settings/index.vue</files>
  <action>
    Update the tabs array in app/pages/protected/settings/index.vue:

    1. Add contrasena tab entry to tabs array:
       - label: 'Contraseña'
       - icon: 'i-heroicons-key' (or 'i-heroicons-lock-closed')
       - value: 'password'
       - to: '/protected/settings/contrasena'
       - description: 'Cambiar tu contraseña'

    2. Update currentTab computed to include contrasena detection:
       - Add check for path.includes('/settings/contrasena') returning 'password'

    3. Verify tab order: General, Códigos QR, Perfil, Contraseña
  </action>
  <verify>
    1. Tabs array includes contrasena entry with correct properties
    2. currentTab computed handles /settings/contrasena route
    3. Tab navigation works correctly
  </verify>
  <done>
    - Contraseña tab appears in settings navigation
    - Clicking Contraseña tab navigates to /protected/settings/contrasena
    - Tab is highlighted correctly when on contrasena page
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete contrasena.vue page with password change form integrated into settings navigation</what-built>
  <how-to-verify>
    1. Start dev server: bun dev
    2. Navigate to http://localhost:3000/protected/settings
    3. Click on "Contraseña" tab
    4. Verify:
       - Page loads without errors
       - Three password fields display (actual, nueva, confirmar)
       - Password fields show/hide toggle works
       - Submit button disabled when form invalid
       - Fill in all fields with mismatched newPassword/confirmPassword
       - Verify inline error appears or submit is blocked
       - Fill with matching passwords, submit
       - Verify success toast appears and form clears
       - Try logging out and in with new password to confirm
    5. Test error handling:
       - Try incorrect current password
       - Verify error toast appears
  </how-to-verify>
  <resume-signal>Type "approved" if password form works correctly, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Navigate to /protected/settings/contrasena - page loads without console errors
2. Password form displays with three fields (current, new, confirm)
3. Password fields have show/hide toggle
4. Client-side validation prevents mismatched passwords
5. Submit button calls changePassword API correctly
6. Success clears form and shows confirmation toast
7. Error handling with Spanish messages for wrong current password
8. Tab navigation properly integrates contrasena page into settings
</verification>

<success_criteria>
1. Users can change password with current password verification
2. Password confirmation prevents typos
3. Success feedback confirms password change
4. Error feedback clarifies wrong current password
5. Form clears after successful change
6. Tab navigation works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-profile-settings/02-02-SUMMARY.md`
</output>
