---
phase: 02-user-profile-settings
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/composables/useQRList.ts
  - app/pages/protected/settings/codigos-qr.vue
  - app/pages/protected/settings/index.vue
autonomous: false
must_haves:
  truths:
    - "Users can see list of all their QR codes from all plans"
    - "Each QR code shows plan name, status (active/disabled/expired), and expiration date"
    - "Users can regenerate QR codes with new access token"
    - "Users can enable/disable QR codes"
    - "QR codes show visual badges for status"
  artifacts:
    - path: "app/composables/useQRList.ts"
      provides: "Composable for QR code list management"
      min_lines: 60
    - path: "app/pages/protected/settings/codigos-qr.vue"
      provides: "QR codes list page with actions"
      min_lines: 100
    - path: "app/pages/protected/settings/index.vue"
      provides: "Updated tabs with codigos-qr tab"
      contains: "to: '/protected/settings/codigos-qr'"
  key_links:
    - from: "app/composables/useQRList.ts"
      to: "/api/planes"
      via: "GET request with userId filter"
      pattern: "\\$fetch.*\\/api\\/planes.*userId"
    - from: "app/pages/protected/settings/codigos-qr.vue"
      to: "/api/planes/[id]/regenerate-qr"
      via: "POST request"
      pattern: "\\$fetch.*regenerate-qr.*POST"
    - from: "app/pages/protected/settings/codigos-qr.vue"
      to: "/api/planes/[id]/toggle-qr"
      via: "POST request"
      pattern: "\\$fetch.*toggle-qr.*POST"
---

<objective>
Create the Códigos QR (QR Codes) tab in settings with list of all user's QR codes and management actions

Purpose: Allow users to view, regenerate, enable/disable all QR codes across their plans
Output: Working codigos-qr.vue page with useQRList composable for QR code management
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-user-profile-settings/02-RESEARCH.md
@.planning/phases/02-user-profile-settings/02-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/pages/protected/settings/index.vue
@app/pages/protected/settings/qr.vue
@app/stores/user.ts
@server/api/planes.get.ts
@server/api/planes/[id]/regenerate-qr.post.ts
@server/api/planes/[id]/toggle-qr.post.ts
@server/api/planes/[id]/generate-qr.post.ts
@server/models/Planes.ts
@app/components/qr/QRStatusBadge.vue
@app/components/qr/QRPreview.vue
</context>

<tasks>

<task type="auto">
  <name>Create useQRList composable for QR management</name>
  <files>app/composables/useQRList.ts</files>
  <action>
    Create useQRList.ts composable following existing composable patterns (useUserStore, useVerification):

    1. State management:
       - loading: ref(false)
       - error: ref<string | null>(null)
       - plansWithQR: ref<IPlan[]>([])
       - userStore: useUserStore()
       - toast: useToast()

    2. Methods:
       a) async loadPlansWithQR(): Fetch user's plans and filter for QR codes
          - Call /api/planes with userId filter from userStore.user._id
          - Filter results to include only plans with qrCode field
          - Handle errors with toast notification
          - Sort by updatedAt descending (newest first)

       b) async regenerateQR(planId: string): Regenerate QR code with new token
          - POST to /api/planes/${planId}/regenerate-qr
          - Show success toast: "Código QR regenerado correctamente"
          - Reload plans list on success
          - Handle errors with toast notification

       c) async toggleQR(planId: string, enabled: boolean): Enable/disable QR code
          - POST to /api/planes/${planId}/toggle-qr with { enabled }
          - Show success toast: "QR {habilitado|deshabilitado} correctamente"
          - Reload plans list on success
          - Handle errors with toast notification

       d) async generateQR(planId: string, expirationDays?: number): Generate QR for plan without QR
          - POST to /api/planes/${planId}/generate-qr with settings
          - Show success toast: "Código QR generado correctamente"
          - Reload plans list on success
          - Handle errors with toast notification

    3. Computed properties:
       - hasQRCodes: plansWithQR.length > 0
       - activeCount: plansWithQR.filter(p => p.qrEnabled && p.qrCode?.expiresAt > new Date()).length
       - expiredCount: plansWithQR.filter(p => p.qrCode?.expiresAt <= new Date()).length

    4. Auto-load on mount (optional parameter)
       - Load plans when composable is created

    5. TypeScript types:
       - Use IPlan from server/types/plan.ts (or import from existing types)
       - Proper typing for all methods

    6. Export the composable function returning state and methods
  </action>
  <verify>
    1. File exists at app/composables/useQRList.ts
    2. Exports loadPlansWithQR, regenerateQR, toggleQR, generateQR methods
    3. Has loading, error, plansWithQR state
    4. Uses useUserStore and useToast
    5. Has computed properties for QR statistics
  </verify>
  <done>
    - useQRList composable exists
    - All CRUD operations for QR codes implemented
    - Error handling with toast notifications
    - Auto-load functionality on mount
  </done>
</task>

<task type="auto">
  <name>Create codigos-qr.vue page with QR list</name>
  <files>app/pages/protected/settings/codigos-qr.vue</files>
  <action>
    Create codigos-qr.vue page following the qr.vue pattern:

    1. Page structure:
       - Page header with title "Mis Códigos QR" and description
       - Statistics cards (UCard) showing:
         * Total QR codes
         * Active QR codes
         * Expired/disabled QR codes
       - Loading and error states matching qr.vue pattern

    2. QR Codes list:
       - UTable or custom list layout showing:
         * Plan name (nom_obra)
         * QR status badge (use QRStatusBadge component)
         * Expiration date (formatted)
         * Actions (regenerate, enable/disable buttons)
       - Empty state when no QR codes exist:
         * Icon: i-heroicons-qrcode
         * Message: "No tienes códigos QR generados"
         * Link to planes page: "Ir a Mis Planes"

    3. Each QR code item displays:
       - Plan name (nom_obra or ob_obra_nom)
       - QRStatusBadge showing state (active/disabled/expired)
       - Expiration date formatted in Spanish locale
       - Action buttons:
         * Regenerate (i-heroicons-arrow-path) - calls regenerateQR
         * Enable/Disable toggle (i-heroicons-check/no-symbol) - calls toggleQR
       - UCard container for each item

    4. Form behavior:
       - Use useQRList composable for state and methods
       - Load QR codes on mount
       - Show loading spinner during load
       - Show error message if load fails
       - Refresh button to reload list

    5. Date formatting:
       - Use Intl.DateTimeFormat for Spanish locale
       - Show "Expira el {date}" for active QRs
       - Show "Expiró el {date}" for expired QRs

    6. Nuxt UI v4 components:
       - UCard for list items
       - UButton for actions
       - UIcon for icons
       - UBadge or QRStatusBadge for status
       - UAlert for info/error banners
       - UTable for list view (optional, can use custom layout)

    7. QRStatusBadge integration:
       - Import and use existing QRStatusBadge component
       - Calculate state from qrCode.expiresAt and qrEnabled

    8. Spanish labels and helper text throughout
  </action>
  <verify>
    1. File exists at app/pages/protected/settings/codigos-qr.vue
    2. Component uses Nuxt UI v4 components
    3. Uses useQRList composable
    4. Uses QRStatusBadge component for status display
    5. Has loading, error, and empty states
  </verify>
  <done>
    - codigos-qr.vue page exists with QR list
    - Statistics cards show QR counts
    - List displays all plans with QR codes
    - Each item shows status badge and expiration
    - Regenerate button works
    - Enable/disable toggle works
    - Empty state shows when no QR codes
  </done>
</task>

<task type="auto">
  <name>Update settings index.vue with codigos-qr tab</name>
  <files>app/pages/protected/settings/index.vue</files>
  <action>
    Update the tabs array in app/pages/protected/settings/index.vue:

    1. The "Códigos QR" tab already exists in the array
    2. Update the to value to point to the new codigos-qr page:
       - to: '/protected/settings/codigos-qr'
    3. Verify currentTab computed handles the route correctly

    Note: The QR settings page (/protected/settings/qr) is for QR configuration (autoGenerate, baseUrl, expirationDays).
    The new codigos-qr page is for listing and managing existing QR codes. Both serve different purposes.
  </action>
  <verify>
    1. Tabs array includes codigos-qr entry
    2. currentTab computed handles /settings/codigos-qr route
    3. Tab navigation works correctly
  </verify>
  <done>
    - Códigos QR tab appears in settings navigation
    - Clicking tab navigates to /protected/settings/codigos-qr
    - Tab is highlighted correctly when on codigos-qr page
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete codigos-qr.vue page with QR list management integrated into settings navigation</what-built>
  <how-to-verify>
    1. Start dev server: bun dev
    2. Navigate to http://localhost:3000/protected/settings
    3. Click on "Códigos QR" tab
    4. Verify:
       - Page loads without errors
       - Statistics cards show correct counts
       - QR codes list displays with plan names
       - Status badges show correct colors (green=active, red=disabled/expired)
       - Expiration dates are formatted correctly
       - Click "Regenerar" on a QR code
       - Verify success toast appears
       - Verify QR code status updates
       - Click "Deshabilitar" on an active QR code
       - Verify success toast appears
       - Verify status badge changes to disabled
       - Click "Habilitar" to re-enable
       - Verify status changes back to active
    5. Test empty state:
       - If no QR codes exist, verify empty state displays
       - Click link to "Mis Planes" and verify navigation
  </how-to-verify>
  <resume-signal>Type "approved" if QR list management works correctly, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Navigate to /protected/settings/codigos-qr - page loads without console errors
2. Statistics cards display accurate QR counts
3. QR codes list shows all user's QR codes from all plans
4. Each QR code item shows: plan name, status badge, expiration date
5. QRStatusBadge shows correct state colors
6. Regenerate button creates new QR code with updated token
7. Enable/disable toggle works correctly
8. Toast notifications for all actions
9. Empty state displays when no QR codes exist
10. Spanish labels throughout
</verification>

<success_criteria>
1. Users can see all their QR codes in one place
2. Users can regenerate QR codes with new tokens
3. Users can enable/disable QR codes
4. Status badges accurately reflect QR code state
5. Statistics show accurate counts
6. All actions provide feedback via toast notifications
7. Tab navigation properly integrates codigos-qr page into settings
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-profile-settings/02-03-SUMMARY.md`
</output>
