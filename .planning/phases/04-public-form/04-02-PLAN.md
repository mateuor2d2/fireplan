---
phase: 04-public-form
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/pages/public/issues/[qrSlug]/[code].vue
  - app/components/IssueReportForm.vue
autonomous: true

must_haves:
  truths:
    - "Public form accessible at /issues/{qrSlug}/{code} route"
    - "Multi-step form collects: Personal info, Issue details, Photo upload, Location, Verification"
    - "Client-side validation prevents invalid submissions"
    - "Photos upload to S3 before form submission"
    - "Confirmation page displays reference number after successful submission"
    - "Form validates QR code and verification code before access"
  artifacts:
    - path: "app/pages/public/issues/[qrSlug]/[code].vue"
      provides: "Public issue reporting page route"
      min_lines: 100
    - path: "app/components/IssueReportForm.vue"
      provides: "Multi-step issue reporting form component"
      min_lines: 150
  key_links:
    - from: "app/pages/public/issues/[qrSlug]/[code].vue"
      to: "server/api/public/issue-report/[planId]/[slug].get.ts"
      via: "fetch QR validation on mount"
    - from: "app/components/IssueReportForm.vue"
      to: "app/schemas/issue-reporting.ts"
      via: "Zod validation before submission"
    - from: "app/components/IssueReportForm.vue"
      to: "server/api/public/issue-report/submit.post.ts"
      via: "POST to /api/public/issue-report/submit"
    - from: "app/components/IssueReportForm.vue"
      to: "app/composables/useS3.ts"
      via: "uploadFile() for photo uploads"
---

<objective>
Create public-accessible multi-step form for issue reporting via QR code.

Purpose: Provide site visitors with an intuitive form to report safety issues, collect verification details, upload photos, and receive confirmation with reference number for tracking.

Output: Working Vue 3 page with multi-step form, validation, S3 photo uploads, and confirmation display.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/components/qr/IssueQRPreview.vue
@app/composables/useS3.ts
@app/schemas/issue-reporting.ts
@app/types/issue-reporting.ts
@server/api/public/issue-report/submit.post.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public issue reporting page route</name>
  <files>app/pages/public/issues/[qrSlug]/[code].vue</files>
  <action>Create Vue 3 page component for public issue reporting:

Route structure: app/pages/public/issues/[qrSlug]/[code].vue

Page logic (script setup):
- Extract qrSlug and code from route params
- Validate QR access by fetching GET /public/issue-report/{planId}/{slug}?token={code} on mount
- Handle validation errors (404, 403, 410) with user-friendly messages
- Show loading state while validating
- Pass validated plan data to IssueReportForm component
- Handle form submission and display confirmation with reference number
- Use Nuxt UI components for styling (UCard, UButton, UAlert, etc.)

Template structure:
- Header with plan name (nom_obra)
- Loading state while validating QR
- Error display if QR validation fails
- IssueReportForm component (when valid)
- Confirmation view after successful submission
- Link back to scan QR again

Follow pattern from app/pages/public/planes/[id]/[slug].vue (public QR access page).
Use useS3 composable for photo uploads (already exists).
</action>
  <verify>Page route works, QR validation called on mount, form displays when valid</verify>
  <done>Public page accessible and validates QR code before showing form</done>
</task>

<task type="auto">
  <name>Task 2: Create multi-step issue reporting form component</name>
  <files>app/components/IssueReportForm.vue</files>
  <action>Create Vue 3 component with multi-step form using Nuxt UI:

Form steps (state managed with currentStep ref):
1. Personal Info: name (required), email (required, email validation), phone (optional, phone format)
2. Issue Details: title (required, min 5 chars), description (required, min 20 chars), type (select: annotation/comment/accident), priority (select: low/medium/high/critical)
3. Photo Upload: use useS3 composable, show preview thumbnails, allow multiple photos
4. Location: building (optional), floor (optional), area (optional)
5. Verification: 6-digit code input, send button, verify button (Phase 5 will implement sending)

Component structure:
- Use Nuxt UI form components (UInput, UTextarea, USelect, UButton)
- Use Zod schemas for client-side validation (reuse from app/schemas/issue-reporting.ts)
- Step navigation: Previous/Next buttons, step indicator
- Form state: formData ref with all fields
- Validation errors: errors ref, display per field
- Submit button on final step
- Loading state during submission

Props:
- planData: Plan info (nom_obra, obraId)
- qrSlug: QR slug for API calls
- accessToken: Access token for API calls
- onSuccess: Callback(referenceNumber) for parent

Emits:
- submit: Emitted when form is submitted with formData
- cancel: Emitted if user wants to cancel

Follow pattern from app/components/qr/IssueQRPreview.vue (Nuxt UI components, props/emits pattern).
Use useS3 composable: const { uploadFile } = useS3()
</action>
  <verify>Form component renders with all steps, validation works, photos upload correctly</verify>
  <done>Multi-step form working with validation and S3 uploads</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Public issue reporting form at /public/issues/{qrSlug}/{code} with multi-step form, validation, photo upload, and confirmation page</what-built>
  <how-to-verify>
    1. Run: bun dev
    2. Generate an issue QR code for a plan (via existing QR generation)
    3. Visit the public URL: /issues/{qrSlug}/{accessToken}
    4. Verify page loads and validates QR code
    5. Test form flow:
       - Fill in personal info (name, email, phone)
       - Fill in issue details (title, description, type, priority)
       - Upload photo (verify S3 upload works)
       - Fill location (optional fields)
       - Enter verification code (may fail until Phase 5, but form should accept input)
    6. Submit form (may fail if verification not implemented, but should validate inputs)
    7. Check responsive design on mobile (< 768px width)
    8. Verify error messages display for invalid inputs
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without errors: bun typecheck
- [ ] Page route exists and validates QR code
- [ ] IssueReportForm component with 5 steps created
- [ ] Client-side validation prevents invalid submissions
- [ ] Photo upload integrates with S3
- [ ] Confirmation page displays reference number
- [ ] User verification passed (form works end-to-end)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors introduced
- Public form accessible via QR code
- Multi-step form validates each step
- Photos upload to S3 successfully
- Reference number displayed after submission
- User verified visual appearance and responsiveness
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-form/04-02-SUMMARY.md`
</output>
