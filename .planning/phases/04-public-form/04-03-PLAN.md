---
phase: 04-public-form
plan: 03
type: execute
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - server/api/public/s3/upload.post.ts
  - app/composables/useS3.ts
  - app/components/IssueReportForm.vue
autonomous: true
gap_closure: true

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "Photos upload to S3 before form submission"
    - "Public upload endpoint accepts multipart form data without authentication"
    - "Uploads are rate-limited to prevent abuse (20 uploads/hour per IP)"
    - "Uploaded files stored in public-issue-uploads folder with proper metadata"
  artifacts:
    - path: "server/api/public/s3/upload.post.ts"
      provides: "Public S3 upload endpoint without authentication"
      min_lines: 50
      exports: ["POST"]
    - path: "server/utils/rateLimit.ts"
      provides: "Rate limiting middleware for public endpoints"
      contains: "createRateLimitMiddleware"
    - path: "app/composables/useS3.ts"
      provides: "S3 upload composable with public endpoint support"
      contains: "uploadFile"
  key_links:
    - from: "app/components/IssueReportForm.vue"
      to: "/api/public/s3/upload"
      via: "uploadFile() call in handlePhotoUpload"
      pattern: "\\$fetch.*public/s3/upload"
    - from: "server/api/public/s3/upload.post.ts"
      to: "S3 bucket"
      via: "s3Service.uploadFile()"
      pattern: "s3Service\\.uploadFile"
---

<objective>
Create public S3 upload endpoint for anonymous photo uploads in issue reporting form.

Purpose: Close the photo upload authentication gap identified in VERIFICATION.md. The existing /api/s3/upload endpoint requires authentication (event.context.user check), but public issue reporting form users are not logged in.

Output: Public S3 upload endpoint with rate limiting, proper folder structure, and client-side integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap closure context from VERIFICATION.md:
@.planning/phases/04-public-form/04-public-form-VERIFICATION.md

# Existing implementation to reference:
@.planning/phases/04-public-form/04-01-SUMMARY.md
@.planning/phases/04-public-form/04-02-SUMMARY.md
@server/api/s3/upload.post.ts
@server/api/services/s3.service.ts
@app/composables/useS3.ts
@server/utils/rateLimit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public S3 upload endpoint with rate limiting</name>
  <files>server/api/public/s3/upload.post.ts</files>
  <action>
Create POST /api/public/s3/upload endpoint that:
1. Accepts multipart form data with 'file' field (same format as existing /api/s3/upload)
2. Does NOT check event.context.user - this is a public endpoint
3. Applies rate limiting: 20 uploads/hour per IP using createRateLimitMiddleware from server/utils/rateLimit.ts
4. Validates file size (max 10MB) and type (images only: jpeg, jpg, png, webp, gif)
5. Stores files in 'public-issue-uploads/{uuid}' folder (not users/{userId})
6. Adds metadata: uploadedBy='public-issue-report', source='qr-form', obraId from request body if provided
7. Returns same response format as existing endpoint: { success, data: { key, url, originalName, size, mimeType }, message }
8. Includes Spanish error messages for consistency

Rate limit: Import { createRateLimitMiddleware } from '~/server/utils/rateLimit' and apply before file processing.
  </action>
  <verify>
- Endpoint exists at server/api/public/s3/upload.post.ts
- TypeScript compilation passes
- Endpoint does NOT check event.context.user
- Rate limiting middleware applied (20/hour limit)
- File validation includes size and type checks
- Uses s3Service.uploadFile() with 'public-issue-uploads' folder
  </verify>
  <done>
Public S3 upload endpoint accepts image files without authentication, rate-limited to 20/hour per IP, stores in public-issue-uploads folder.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update useS3 composable to support public endpoint</name>
  <files>app/composables/useS3.ts</files>
  <action>
Modify the useS3 composable to support public uploads:

1. Add optional 'public' boolean parameter to uploadFile options: { onProgress, metadata, folder, public = false }
2. When public=true, call '/api/public/s3/upload' instead of '/api/s3/upload'
3. Keep existing behavior when public=false (authenticated upload)
4. Export a new usePublicS3() composable that calls uploadFile with public=true by default

Reference existing code: IssueReportForm.vue calls uploadFile from useS3 but doesn't set public flag - this will be fixed in Task 3.
  </action>
  <verify>
- useS3 uploadFile accepts 'public' option in UploadOptions
- public=true routes to /api/public/s3/upload
- public=false routes to /api/s3/upload (existing behavior)
- usePublicS3() composable exported
- TypeScript compilation passes
  </verify>
  <done>
useS3 composable supports public endpoint via 'public' option. usePublicS3() composable available for convenience.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update IssueReportForm to use public upload endpoint</name>
  <files>app/components/IssueReportForm.vue</files>
  <action>
Modify IssueReportForm.vue to use public S3 upload:

1. Find the handlePhotoUpload function (around line 240-250 in current file)
2. Either: (a) Replace useS3() with usePublicS3(), OR (b) Pass public: true to uploadFile options
3. Add obraId to upload metadata so files are traceable to specific construction projects
4. Keep existing thumbnail preview and remove functionality

Gap reference: VERIFICATION.md identified that IssueReportForm.vue calls uploadFile but /api/s3/upload requires authentication.
  </action>
  <verify>
- IssueReportForm.vue uses public upload endpoint
- obraId passed in upload metadata
- Photo upload UI triggers public endpoint
- TypeScript compilation passes
- No authentication required for photo upload
  </verify>
  <done>
IssueReportForm.vue uploads photos via public endpoint without authentication. Photos include obraId metadata for traceability.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Public S3 upload endpoint exists at /api/public/s3/upload
- [ ] Endpoint accepts image files (jpeg, jpg, png, webp, gif) up to 10MB
- [ ] Rate limiting applied (20 uploads/hour per IP)
- [ ] useS3 composable supports 'public' option
- [ ] IssueReportForm.vue uses public upload endpoint
- [ ] Files stored in 'public-issue-uploads' folder
- [ ] Test: Upload succeeds without authentication
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Photos upload successfully from public form without login
- Rate limiting prevents abuse
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-form/04-03-SUMMARY.md`
</output>
