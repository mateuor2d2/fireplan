---
phase: 04.2-add-issues-icon-to-elementbase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/components/ElementBase.vue]
autonomous: false

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "User can see issues icon button in planes list items"
    - "Clicking issues icon navigates directly to /protected/planes/{id}/issues"
    - "Issues icon button only appears for planes (not conceptos)"
  artifacts:
    - path: "app/components/ElementBase.vue"
      provides: "Issues dashboard navigation button"
      min_lines: 537
  key_links:
    - from: "app/components/ElementBase.vue"
      to: "/protected/planes/{id}/issues"
      via: "navigateTo in goToIssues function"
      pattern: "navigateTo.*issues"
---

<objective>
Add issues dashboard icon button to ElementBase component for direct navigation to issues management from planes list.

Purpose: Users need quick access to issues management dashboard from the planes list without navigating through the general dashboard first.

Output: Updated ElementBase.vue with new issues button that navigates directly to /protected/planes/{id}/issues.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase completion - Phase 4.1 created the dedicated issues page
@.planning/phases/04.1-fix-issues-dashboard-routing/04.1-01-SUMMARY.md

# Target component to modify
@app/components/ElementBase.vue

# Existing patterns from Phase 4.1
- Dedicated issues page route: /protected/planes/[id]/issues
- Navigation pattern: navigateTo(`/protected/planes/${planId}/issues`)
- Icon pattern: i-heroicons-exclamation-triangle for issues-related UI
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add goToIssues function to ElementBase</name>
  <files>app/components/ElementBase.vue</files>
  <action>
    Add a new function `goToIssues(itemId: string)` in the script section after the `goToDashboard` function (around line 372).

    The function should:
    - Accept itemId: string parameter
    - Validate itemId exists (console.warn if undefined)
    - Check props.tipo === 'planes' (console.warn for conceptos)
    - Use navigateTo to go to `/protected/planes/${itemId}/issues`

    Pattern: Follow the same structure as the existing goToDashboard function for consistency.
  </action>
  <verify>TypeScript compiles without errors, function is defined in script setup</verify>
  <done>goToIssues function defined with proper navigation logic and validation</done>
</task>

<task type="auto">
  <name>Task 2: Add issues icon button to template</name>
  <files>app/components/ElementBase.vue</files>
  <action>
    Add a new UTooltip + UButton pair after the existing dashboard button (line 516) in the template.

    Structure should match existing button pattern:
    ```vue
    <UTooltip text="Incidencias">
      <UButton
        icon="i-heroicons-exclamation-circle"
        size="sm"
        color="error"
        square
        variant="solid"
        @click.stop="goToIssues((item as any)._id)"
      />
    </UTooltip>
    ```

    Key differences from dashboard button:
    - Icon: i-heroicons-exclamation-circle (exclamation icon, more clearly indicates issues)
    - Color: "error" (red/danger color to indicate issues)
    - Variant: "solid" (more prominent than ghost)
    - Tooltip: "Incidencias" (Spanish for Issues)

    This button should:
    - Only show for planes (use v-if="props.tipo === 'planes'")
    - Use stop propagation on click (@click.stop)
    - Follow Nuxt UI button patterns in ElementBase
  </action>
  <verify>npm run build succeeds, no TypeScript errors</verify>
  <done>Issues icon button added to template with proper styling and tooltip</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ElementBase.vue updated with issues icon button and goToIssues navigation function</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/protected/planes
    3. Verify: Each plan item in the list shows a red exclamation icon button (after the warning dashboard icon)
    4. Click: The red exclamation icon on any plan
    5. Confirm: Navigation goes directly to /protected/planes/{id}/issues (issues dashboard)
    6. Verify: Conceptos list does NOT show the issues icon button (only for planes)
    7. Hover: Verify tooltip shows "Incidencias" when hovering over the red icon
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] No TypeScript compilation errors
- [ ] goToIssues function properly validates and navigates
- [ ] Issues icon button visible only for planes items
- [ ] User confirms navigation works from planes list to issues page
</verification>

<success_criteria>

- All tasks completed
- User verification passed (navigation works correctly)
- Issues icon button only appears for planes (not conceptos)
- Button styling matches Nuxt UI patterns in ElementBase
- Navigation directly to issues dashboard works
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-add-issues-icon-to-elementbase/04.2-01-SUMMARY.md`
</output>
