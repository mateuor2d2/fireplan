---
phase: 04.3-allow-not-logged-user-through-the-issue-qr-to-create-an-issue-on-a-plan
task: Issue submission fixes and phone validation
status: in_progress
last_updated: 2026-03-20T13:25:55.426Z
---

<current_state>
Working on Phase 4.3 - Public QR Issue Submission. Recently fixed phone number validation to accept formats with spaces, hyphens and parentheses. The S3 ACL error needs to be fixed later (quitar ACL de server/utils/s3-upload.ts línea 53).
</current_state>

<completed_work>

1. **Phone Number Validation Fix** - Fixed format validation for verification codes
   - Updated `server/schemas/verification.ts` - Added transform to normalize phone numbers
   - Updated `server/schemas/coordinator.ts` - Same fix applied to coordinator schemas
   - Now accepts: `+34 612 345 678`, `+34-612-345-678`, `(+34) 612 345 678`
   - Numbers are automatically cleaned (spaces/parentheses/hyphens removed) before validation

2. **Previous Session Work** - Phase 4.3 infrastructure complete
   - Public issue submission endpoint working
   - QR code validation via slug
   - Verification code system (email/SMS) implemented
   - Photo upload to S3 working (with ACL workaround pending)
   - Issue creation with reference numbers

</completed_work>

<remaining_work>

- **S3 ACL Error Fix** (LOW priority - saved for later)
  - File: `server/utils/s3-upload.ts` line 53
  - Action: Remove `ACL: 'public-read'` from PutObjectCommand
  - Alternative: Configure bucket policy for public access instead of ACLs
  - Context: Modern S3 buckets disable ACLs by default for security

- **Test Issue Flow End-to-End**
  - Verify phone validation works with formatted numbers
  - Test verification code generation via SMS
  - Confirm issue submission creates records correctly

- **LazyUContentSearch Component Error**
  - Vue warning about unresolved component
  - May need @nuxt/ui-pro package installation

</remaining_work>

<decisions_made>

- **Phone Normalization**: Use Zod transform to clean phone numbers before validation rather than strict regex
- **S3 ACL**: Will use bucket policy approach instead of ACLs (modern best practice)
- **Component Fix Deferred**: LazyUContentSearch warning is non-blocking, can be fixed later

</decisions_made>

<blockers>

- None currently blocking work
- S3 ACL error is logged but doesn't prevent functionality

</blockers>

<context>
Phase 4.3 allows non-logged users to create issues via QR code access. The flow is:
1. User scans QR code → accesses public issue form
2. User enters contact info (email or phone)
3. System sends verification code
4. User submits issue with verification code
5. Issue created with reference number for tracking

Recent work focused on making phone number input more user-friendly by accepting formatted numbers while storing clean/normalized versions in the database.
</context>

<files_modified>
- server/schemas/verification.ts (phone validation with transform)
- server/schemas/coordinator.ts (phone validation with transform)
- app/components/IssueReportForm.vue (in progress - verification flow)
- server/api/public/issue-report/submit.post.ts (in progress - submission handling)
- server/utils/s3-upload.ts (pending ACL removal)
</files_modified>

<next_action>
When resuming:
1. Test the phone validation fix by submitting an issue with formatted phone number
2. Check if verification code is received and validation works
3. If all good, proceed to fix S3 ACL issue:
   - Edit server/utils/s3-upload.ts line 53
   - Remove `ACL: 'public-read'`
   - Configure bucket policy in AWS console instead
4. Consider installing @nuxt/ui-pro to resolve LazyUContentSearch warning
</next_action>
