---
phase: 04.3-allow-not-logged-user-through-the-issue-qr-to-create-an-issue-on-a-plan
plan: 01
type: execute
wave: 1
depends_on: ["04-02", "04-03"]
files_modified:
  - app/schemas/issue-reporting.ts
  - server/api/public/issue-report/submit.post.ts
  - app/components/IssueReportForm.vue
  - app/pages/public/issues/[qrSlug]/[code].vue
autonomous: false

must_haves:
  truths:
    - "QR code leads directly to issue form without authentication"
    - "Anonymous users can submit issues with valid QR token"
    - "Verification code is optional for submission"
    - "Reference number generated and provided for tracking"
    - "Rate limiting prevents abuse"
    - "Existing authenticated flow still works"
  artifacts:
    - path: "app/schemas/issue-reporting.ts"
      provides: "IssueReportSubmitSchema with optional verificationCode"
      contains: "verificationCode.*optional"
    - path: "server/api/public/issue-report/submit.post.ts"
      provides: "Public submission endpoint with optional verification"
      contains: "verificationCode.*optional"
    - path: "app/components/IssueReportForm.vue"
      provides: "Form component with optional verification step"
      contains: "verificationCode.*optional"
    - path: "app/pages/public/issues/[qrSlug]/[code].vue"
      provides: "Public issue page with optional verification"
      min_lines: 100
  key_links:
    - from: "app/pages/public/issues/[qrSlug]/[code].vue"
      to: "server/api/public/issue-report/submit.post.ts"
      via: "handleSubmit function"
      pattern: "POST /api/public/issue-report/submit"
    - from: "app/components/IssueReportForm.vue"
      to: "app/schemas/issue-reporting.ts"
      via: "IssueReportSubmitSchema validation"
    - from: "server/api/public/issue-report/submit.post.ts"
      to: "server/models/VerificationCode"
      via: "conditional check based on verificationCode presence"
---

<objective>
Make verification code optional in public issue submission to allow anonymous users to report issues via QR code without requiring email/SMS verification.

Purpose: Enable immediate issue reporting through QR codes while Phase 2 (Verification System) is not yet implemented. Users can submit issues with valid QR token, and verification becomes optional for future enhancement.

Output: Working anonymous issue submission flow with optional verification code, rate limiting, and reference number tracking.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-form/04-02-SUMMARY.md
@.planning/phases/04-public-form/04-03-SUMMARY.md
@.planning/phases/04.1-fix-issues-dashboard-routing/04.1-01-SUMMARY.md
@app/schemas/issue-reporting.ts
@server/api/public/issue-report/submit.post.ts
@app/components/IssueReportForm.vue
@app/pages/public/issues/[qrSlug]/[code].vue
@server/models/VerificationCode.ts
@server/models/Issue.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make verification code optional in Zod schema</name>
  <files>app/schemas/issue-reporting.ts</files>
  <action>Modify IssueReportSubmitSchema to make verificationCode optional:

Current (lines 99-101):
```typescript
verificationCode: z.string()
  .length(6, 'Código de verificación debe ser exactamente 6 dígitos')
  .regex(/^\d{6}$/, 'Código de verificación debe contener solo números'),
```

Change to:
```typescript
verificationCode: z.string()
  .length(6, 'Código de verificación debe ser exactamente 6 dígitos')
  .regex(/^\d{6}$/, 'Código de verificación debe contener solo números')
  .optional(),
```

This allows the submit endpoint to receive requests without verificationCode while maintaining validation when the code IS provided.
</action>
  <verify>TypeScript compiles without errors, schema accepts submissions with and without verificationCode</verify>
  <done>Schema accepts optional verification code</done>
</task>

<task type="auto">
  <name>Task 2: Update submit endpoint to handle optional verification</name>
  <files>server/api/public/issue-report/submit.post.ts</files>
  <action>Modify submit.post.ts to make verification code optional:

1. Change Step 3 (lines 98-124) from required to conditional:
   - Wrap verification code lookup in conditional: `if (verificationCode) { ... }`
   - Only validate verification code if provided
   - Only mark verification code as used if provided
   - Skip verification code validation entirely if not provided

2. Add rate limiting for abuse prevention (20 submissions/hour per IP):
   - Import createRateLimitMiddleware from server/middleware/rateLimit
   - Apply rate limit middleware to endpoint
   - Use stricter limit than verification endpoints (20/hour vs 100/hour)

3. Update issue creation to track verification method:
   - Add field: `verified: verificationCode ? 'code' : 'pending'`
   - This allows coordinators to see which issues need verification

Key changes:
- Lines 99-122: Wrap in `if (verificationCode) { ... }`
- Add rate limiting middleware
- Update Issue.create() to include verified field
- Update console.log to indicate verification status

Follow existing pattern from server/api/public/s3/upload.post.ts for rate limiting.
</action>
  <verify>Endpoint accepts submissions with and without verificationCode, rate limiting applied, Issue model includes verified field</verify>
  <done>Submit endpoint handles optional verification with rate limiting</done>
</task>

<task type="auto">
  <name>Task 3: Update form component to make verification optional</name>
  <files>app/components/IssueReportForm.vue</files>
  <action>Modify IssueReportForm.vue to make verification step optional:

1. Update form validation (lines 433-436):
   - Change stepValidation[5] to make verificationCode optional
   - Update canProceed computed to skip verification check

2. Update Step 5 UI (lines 232-271):
   - Change field label from "required" to optional
   - Update helper text to indicate verification is optional
   - Change "Verificación requerida" alert to "Verificación opcional"
   - Update button text from "Próximamente" to "Omitir verificación"

3. Update submit handling (lines 501-549):
   - Allow submission even if verificationCode is empty
   - Pass empty string or omit verificationCode if not provided

4. Update step indicator to show verification as optional:
   - Add visual indicator (e.g., "(opcional)") to step label

This provides better UX by making it clear that users can skip verification if they don't have a code.
</action>
  <verify>Form allows submission without verification code, UI clearly indicates optional step, validation accepts empty verificationCode</verify>
  <done>Form component handles optional verification</done>
</task>

<task type="auto">
  <name>Task 4: Update public page to handle optional verification</name>
  <files>app/pages/public/issues/[qrSlug]/[code].vue</files>
  <action>Update public issue page to reflect optional verification:

1. Update InfoAlert (lines 111-118) to clarify verification is optional:
   - Current: "Todos los reportes son verificados..."
   - Change to: "Los reportes pueden ser verificados opcionalmente para confirmar tu identidad..."

2. Add optional hint about verification for future enhancement:
   - Add small note: "La verificación por email/SMS estará disponible pronto"

3. Ensure confirmation message (lines 47-93) reflects optional verification:
   - Update message if no verification was provided
   - Reference number still provided for tracking

This ensures user expectations are properly set regarding optional verification.
</action>
  <verify>Page text reflects optional verification, confirmation message appropriate for unverified submissions</verify>
  <done>Public page reflects optional verification</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Anonymous issue submission flow with optional verification code, rate limiting, and reference number tracking</what-built>
  <how-to-verify>
    1. Run: bun dev
    2. Generate an issue QR code for a plan (via existing QR generation)
    3. Visit the public URL: /issues/{qrSlug}/{accessToken}
    4. Verify page loads and validates QR code
    5. Test submission WITHOUT verification code:
       - Fill in personal info (name, email, phone)
       - Fill in issue details (title, description, type, priority)
       - Optionally upload photo
       - Skip verification step (leave code empty) or omit it
       - Submit form
    6. Verify:
       - Issue is created successfully
       - Reference number is displayed
       - Success message appears
    7. Test submission WITH verification code (if you want):
       - Enter a 6-digit code
       - Verify submission works (will fail if code doesn't exist, but should accept input)
    8. Check rate limiting:
       - Try submitting multiple times rapidly
       - After 20 submissions, should be rate limited
    9. Verify responsive design on mobile
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without errors: bun typecheck
- [ ] Schema accepts optional verificationCode
- [ ] Submit endpoint handles both verified and unverified submissions
- [ ] Rate limiting applied (20/hour per IP)
- [ ] Form allows submission without verification code
- [ ] Page text reflects optional verification
- [ ] User verification passed (form submits successfully without code)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors introduced
- Anonymous users can submit issues via QR without verification code
- Reference number generated for tracking
- Rate limiting prevents abuse (20/hour per IP)
- Form and page UI clearly indicate optional verification
- User verified end-to-end submission flow
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-allow-not-logged-user-through-the-issue-qr-to-create-an-issue-on-a-plan/04.3-01-SUMMARY.md`
</output>
