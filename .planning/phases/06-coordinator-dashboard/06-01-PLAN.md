---
phase: 06-coordinator-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/protected/planes/[[id]]/issues.vue
  - app/pages/protected/planes/index.vue
  - server/middleware/auth.ts
autonomous: true

must_haves:
  truths:
    - "User can navigate from planes table to issues management page"
    - "Issues page is accessible via QR token without authentication"
    - "Issues page remains accessible to authenticated users"
    - "Navigation links work correctly from all entry points"
  artifacts:
    - path: "app/pages/protected/planes/index.vue"
      provides: "Planes table with issues management link"
      contains: "UButton or navigation link to issues"
    - path: "app/pages/protected/planes/[[id]]/issues.vue"
      provides: "Issues page with dual access (auth + QR token)"
      contains: "QR token validation logic"
    - path: "server/middleware/auth.ts"
      provides: "Authentication middleware with QR token bypass"
      exports: ["defineEventHandler"]
  key_links:
    - from: "app/pages/protected/planes/index.vue"
      to: "/protected/planes/[id]/issues"
      via: "UButton with navigateTo or NuxtLink"
      pattern: "navigateTo.*issues|NuxtLink.*issues"
    - from: "app/pages/protected/planes/[[id]]/issues.vue"
      to: "server/api/public/issues"
      via: "fetch with QR token validation"
      pattern: "fetch.*api/public/issues"
---

<objective>
Add navigation entry points and dual access control (authentication OR QR token) for the issues management page.

Purpose: Enable coordinators to access issues management both via authenticated login and via QR code token for public access.

Output: Navigation links from planes table, enhanced issues page with QR token access control.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04.1-fix-issues-dashboard-routing/04.1-01-SUMMARY.md
@.planning/phases/04-public-form/04-01-SUMMARY.md
@app/pages/protected/planes/[[id]]/issues.vue
@app/pages/protected/planes/index.vue
@server/middleware/auth.ts
@server/services/issueQRService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add issues management link to planes table</name>
  <files>app/pages/protected/planes/index.vue</files>
  <action>
    Find the planes table component (likely using TableGeneric or similar). Add a new column or action button for "Gestionar Incidencias" that navigates to `/protected/planes/{planId}/issues`.

    Use UButton with icon="i-heroicons-exclamation-circle" (or similar issue icon) and navigateTo() for navigation.
    Add the link in the actions column of each plan row.
  </action>
  <verify>
    - Planes table has a visible button/link to issues management
    - Clicking the link navigates to the issues page for that specific plan
    - TypeScript compilation passes
  </verify>
  <done>Planes table includes navigation button to issues management page</done>
</task>

<task type="auto">
  <name>Task 2: Add QR token access control to issues page</name>
  <files>app/pages/protected/planes/[[id]]/issues.vue</files>
  <action>
    Enhance the issues page to support QR token-based access without authentication:

    1. Add QR token query param extraction: const qrToken = route.query.token as string

    2. Add dual access logic:
       - If user is authenticated (event.context.user), allow access
       - If qrToken is present, validate token via POST /api/public/issue-report/validate-by-slug
       - If neither, redirect to login

    3. Store access method in state: accessType = ref<'auth' | 'qr-token' | null>(null)

    4. Pass accessType to ObraDashboard component for API call differentiation

    Reference the validation pattern from Phase 4 (IssueReportForm.vue) for QR token validation.
  </action>
  <verify>
    - Authenticated users can access issues page
    - QR token allows access without authentication (valid token)
    - Invalid QR token shows error and redirects
    - No authentication + no token redirects to login
  </verify>
  <done>Issues page supports both authenticated and QR token access</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Planes table has visible "Gestionar Incidencias" button/link
- [ ] Clicking link navigates to correct issues page for the plan
- [ ] Authenticated users can access issues page
- [ ] QR token access works (test with valid token from QR code)
- [ ] Invalid/missing token shows appropriate error
- [ ] TypeScript compilation passes
</verification>

<success_criteria>

- Navigation from planes table to issues works correctly
- Dual access control (auth + QR token) functional
- User experience is clear for both access methods
</success_criteria>

<output>
After completion, create `.planning/phases/06-coordinator-dashboard/06-01-SUMMARY.md`
</output>
