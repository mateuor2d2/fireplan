---
phase: 06-coordinator-dashboard
plan: 05
type: execute
wave: 5
depends_on: ["06-03"]
files_modified:
  - server/api/issues/[id]/list.get.ts
  - server/api/issues/export/[id].get.ts
  - app/components/ObraDashboard.vue
autonomous: true

must_haves:
  truths:
    - "Coordinators can filter issues by status, type, priority, and assignedTo"
    - "Filtered results return correct subset of issues"
    - "Issues can be exported to CSV format"
    - "Issues can be exported to PDF format"
  artifacts:
    - path: "server/api/issues/[id]/list.get.ts"
      provides: "Filtered issues list endpoint"
      exports: ["defineEventHandler"]
    - path: "server/api/issues/export/[id].get.ts"
      provides: "Export endpoint for CSV/PDF"
      exports: ["defineEventHandler"]
    - path: "app/components/ObraDashboard.vue"
      provides: "Filter and export UI"
      contains: "Filter controls and export buttons"
  key_links:
    - from: "app/components/ObraDashboard.vue"
      to: "server/api/issues/[id]/list"
      via: "fetch GET with filter query params"
      pattern: "fetch.*issues.*list.*status|priority"
    - from: "app/components/ObraDashboard.vue"
      to: "server/api/issues/export/[id]"
      via: "fetch GET with format param"
      pattern: "fetch.*issues.*export.*format"
---

<objective>
Implement issue filtering by multiple criteria and export functionality for reporting.

Purpose: Enable coordinators to find specific issues quickly through filtering and generate reports in CSV/PDF format for documentation.

Output: Filtering API endpoints and export functionality with UI controls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/06-coordinator-dashboard/06-03-SUMMARY.md
@server/models/Issue.ts
@app/components/ObraDashboard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create filtered issues list endpoint</name>
  <files>server/api/issues/[id]/list.get.ts</files>
  <action>
    Create GET /api/issues/[id]/list endpoint for fetching filtered issues for an obra:

    1. Extract query params with Zod validation:
       - obraId (required): string
       - status (optional): array of status values
       - type (optional): array of type values
       - priority (optional): array of priority values
       - assignedTo (optional): array of coordinator IDs
       - page/limit for pagination (optional)

    2. Build query filter object incrementally:
       - Always include { obraId }
       - Add status filter if provided: { status: { $in: statusArray } }
       - Add type filter if provided: { type: { $in: typeArray } }
       - Add priority filter if provided: { priority: { $in: priorityArray } }
       - Add assignedTo filter if provided: { assignedTo: { $in: assignedToArray } }

    3. Query with filters and sort:
       - Issue.find(filter).sort({ createdAt: -1 })

    4. Apply pagination if page/limit provided

    5. Return filtered issues + count metadata

    Return format: { data: issues[], total: number, page: number }
  </action>
  <verify>
    - Filtering by single criterion works
    - Multiple filters work together (AND logic)
    - Empty filters return all issues for obra
    - Pagination works correctly
    - Invalid filter values return 400
  </verify>
  <done>Filtered issues list endpoint with multi-criteria support</done>
</task>

<task type="auto">
  <name>Task 2: Create export endpoint (CSV/PDF)</name>
  <files>server/api/issues/export/[id].get.ts</files>
  <action>
    Create GET /api/issues/export/[id] endpoint for exporting issues in CSV or PDF format:

    1. Extract query params:
       - obraId (required): string
       - format: 'csv' | 'pdf' (default: 'csv')
       - Same filter params as Task 1 (status, type, priority, assignedTo)

    2. Fetch filtered issues using same logic as Task 1

    3. For CSV format:
       - Generate CSV header row
       - Map issues to CSV rows (title, description, type, status, priority, createdAt, etc.)
       - Set Content-Type: text/csv
       - Set Content-Disposition: attachment; filename="issues-{obraId}-{date}.csv"
       - Return CSV string

    4. For PDF format (optional/basic):
       - Use pdfmake or similar library
       - Create simple table layout
       - Set Content-Type: application/pdf
       - Set Content-Disposition: attachment; filename="issues-{obraId}-{date}.pdf"
       - Return PDF buffer

    Note: Keep PDF simple for now (basic table). Enhanced PDF can be added later if needed.
  </action>
  <verify>
    - CSV export downloads correct file
    - CSV contains all filtered issues
    - CSV headers match data columns
    - PDF export downloads correct file (if implemented)
    - Export filename includes obraId and timestamp
    - Filters apply to exported data correctly
  </verify>
  <done>Export endpoint supporting CSV and PDF formats</done>
</task>

<task type="auto">
  <name>Task 3: Add filter and export UI to ObraDashboard</name>
  <files>app/components/ObraDashboard.vue</files>
  <action>
    Enhance ObraDashboard component with filtering and export UI:

    1. Add filter controls section above issues list:
       - Status multi-select (USelectMultiple with options)
       - Type multi-select
       - Priority multi-select
       - Assigned coordinator multi-select
       - "Limpiar filtros" button to reset all filters

    2. Add export buttons:
       - "Exportar CSV" button (UButton with icon="i-heroicons-document-arrow-down")
       - "Exportar PDF" button (if PDF implemented)
       - Both buttons trigger download via window.open() or blob download

    3. Add reactive filter state:
       - const filters = ref({ status: [], type: [], priority: [], assignedTo: [] })
       - Watch for changes and refetch issues list

    4. Add visual feedback:
       - Show active filter count badge
       - Highlight filtered issues in list
       - Show "No se encontraron incidencias" when filters return empty

    5. Implement debounce for filter changes to avoid excessive API calls
  </action>
  <verify>
    - Filter controls display correctly
    - Selecting filters updates issues list
    - Clear filters button resets all filters
    - Export buttons trigger file downloads
    - Active filter count displays correctly
    - Empty state shows when no results
    - TypeScript compilation passes
  </verify>
  <done>Filter and export UI in ObraDashboard</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Filtered list endpoint created and functional
- [ ] Multiple filters work together correctly
- [ ] Export endpoint generates CSV file
- [ ] Export endpoint generates PDF file (if implemented)
- [ ] Export files download correctly
- [ ] Filter UI added to ObraDashboard
- [ ] Export buttons work from UI
- [ ] Filters apply to both list view and exports
- [ ] TypeScript compilation passes
</verification>

<success_criteria>

- Coordinators can filter issues by multiple criteria
- Export functionality generates CSV/PDF reports
- UI provides intuitive filter and export controls
</success_criteria>

<output>
After completion, create `.planning/phases/06-coordinator-dashboard/06-05-SUMMARY.md`
</output>
