---
phase: 07-database-api-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/models/Subscription.ts
  - server/types/subscription.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Subscription records can be created and retrieved from database"
    - "Subscription model supports all required statuses (active, past_due, canceled, paused, expired)"
    - "Annual prepayment discount is supported in model schema"
    - "Database indexes ensure efficient queries for common patterns"
    - "Payment model already covers STAT-01 (status persistence) and STAT-02 (timestamp + amount)"
  artifacts:
    - path: "server/models/Subscription.ts"
      provides: "Mongoose subscription model with TypeScript interface"
      min_lines: 100
      contains: "ISubscription, SubscriptionSchema, status enum, indexes"
    - path: "server/types/subscription.ts"
      provides: "TypeScript type definitions for subscription operations"
      min_lines: 30
      exports: ["ISubscription", "SubscriptionCreateInput", "SubscriptionUpdateInput"]
    - path: "server/models/Payment.ts"
      provides: "Existing one-time payment model covering STAT-01 and STAT-02"
      min_lines: 0
      contains: "status enum, amount, timestamps"
  key_links:
    - from: "server/models/Subscription.ts"
      to: "server/models/Payment.ts"
      via: "schema pattern reference"
      pattern: "follow Payment.ts schema structure (timestamps, indexes, interface)"
    - from: "server/types/subscription.ts"
      to: "server/models/Subscription.ts"
      via: "import"
      pattern: "import.*ISubscription.*from.*models/Subscription"
---

<objective>
Create the Subscription database model and TypeScript types for QR issue reporting subscription management.

Purpose: Establish the data foundation for per-plan monthly subscriptions with annual prepayment discounts, supporting all Stripe subscription statuses and enabling efficient queries.

Output: Working Subscription model with indexes, TypeScript interfaces, and database validation.
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-database-api-foundation/07-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

# Reference patterns
@server/models/Payment.ts
@server/types/planes.ts
@server/utils/stripe.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Subscription TypeScript types</name>
  <files>server/types/subscription.ts</files>
  <action>
Create TypeScript type definitions for subscriptions at server/types/subscription.ts:

1. Define ISubscription interface extending Document with:
   - _id: string
   - userId: string (ref: User)
   - planId: string (ref: Plan)
   - stripeSubscriptionId: string (unique)
   - stripeCustomerId: string
   - stripePriceId: string
   - status: 'active' | 'past_due' | 'canceled' | 'paused' | 'expired'
   - billingInterval: 'month' | 'year'
   - currentPeriodStart: Date
   - currentPeriodEnd: Date
   - cancelAtPeriodEnd: boolean
   - annualPrepaymentDiscount?: number (10-20, optional)
   - amount: number
   - currency: string
   - metadata?: Record<string, any>
   - createdAt: Date
   - updatedAt: Date

2. Define SubscriptionCreateInput interface for creation operations
3. Define SubscriptionUpdateInput interface for update operations
4. Export all types

Follow the pattern from server/types/planes.ts for consistency.
  </action>
  <verify>File exists with all required interfaces and exports</verify>
  <done>TypeScript compiles without errors, all subscription statuses defined</done>
</task>

<task type="auto">
  <name>Task 2: Create Subscription Mongoose model</name>
  <files>server/models/Subscription.ts</files>
  <action>
Create Mongoose subscription model at server/models/Subscription.ts:

1. Import ISubscription from server/types/subscription.ts
2. Define SubscriptionSchema with all fields from ISubscription interface:
   - userId, planId: required, indexed
   - stripeSubscriptionId: required, unique
   - stripeCustomerId, stripePriceId: required
   - status: enum with ['active', 'past_due', 'canceled', 'paused', 'expired'], default 'active'
   - billingInterval: enum with ['month', 'year'], required
   - currentPeriodStart, currentPeriodEnd: Date, required
   - cancelAtPeriodEnd: Boolean, default false
   - annualPrepaymentDiscount: Number, min 10, max 20, optional
   - amount: Number, required
   - currency: String, required, default 'eur'
   - metadata: Schema.Types.Mixed, default {}
   - Enable timestamps (createdAt, updatedAt)

3. Add indexes for query performance:
   - { userId: 1, planId: 1 } - For user's plan subscriptions
   - { stripeSubscriptionId: 1 } - For webhook lookups (unique via schema)
   - { status: 1 } - For filtering by status
   - { currentPeriodEnd: 1 } - For upcoming renewal queries
   - { createdAt: -1 } - For history views

4. Export Subscription model

Follow the exact pattern from server/models/Payment.ts for consistency.
  </action>
  <verify>
```bash
# Run typecheck to verify no TypeScript errors
bun typecheck

# Verify model compiles
cat server/models/Subscription.ts
```
  </verify>
  <done>Subscription model created with all fields, indexes, and TypeScript interface</done>
</task>

<task type="auto">
  <name>Task 3: Verify Payment model covers STAT-01 and STAT-02 requirements</name>
  <files>server/models/Payment.ts</files>
  <action>
Verify the existing Payment model covers STAT-01 and STAT-02 requirements:

1. Read server/models/Payment.ts to confirm:
   - STAT-01 (Payment status persists): Check that status field exists with enum values ['pending', 'succeeded', 'failed', 'canceled', 'requires_payment_method', 'requires_confirmation', 'requires_action', 'processing']
   - STAT-02 (Payment timestamp and amount): Check that amount field (Number) and timestamps (createdAt, updatedAt via mongoose timestamps: true) exist

2. Confirm model has proper indexes for payment queries:
   - Index on { userId: 1, planId: 1 }
   - Index on { status: 1 }
   - Index on { createdAt: -1 }

3. Verify the model is properly exported and can be imported by API endpoints

4. Add a comment to the task summary confirming STAT-01 and STAT-02 are already covered by the existing Payment model.

No modifications to Payment.ts are needed - this is a verification task only.
  </action>
  <verify>
```bash
# Verify Payment model exists and has required fields
grep -E "(status|amount|timestamps)" server/models/Payment.ts

# Confirm indexes exist
grep -A5 "PaymentSchema.index" server/models/Payment.ts
```
  </verify>
  <done>Payment model verified to support STAT-01 (status persistence) and STAT-02 (timestamp + amount storage)</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. TypeScript compilation passes: `bun typecheck`
2. Subscription.ts model has all required fields and indexes
3. Types are exported from subscription.ts
4. Model follows Payment.ts pattern exactly

Verify with:
```bash
bun typecheck
grep -r "ISubscription" server/types/ server/models/
```
</verification>

<success_criteria>
1. Subscription model can be imported: `import { Subscription } from '~/server/models/Subscription'`
2. TypeScript interface includes all required fields and status enum
3. Database indexes ensure efficient queries for userId+planId lookups
4. Model supports annual prepayment discount (10-20% range)
5. All Stripe subscription statuses are supported
</success_criteria>

<output>
After completion, create `.planning/phases/07-database-api-foundation/07-01-SUMMARY.md` with:
- Files created (2 files)
- Model schema details (fields, indexes, validation)
- Type definitions exported
- Any decisions or deviations from research
</output>
