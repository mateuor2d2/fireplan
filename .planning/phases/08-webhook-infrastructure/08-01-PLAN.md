---
phase: 08-webhook-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/types/webhook.ts
  - server/models/WebhookEvent.ts
autonomous: true

must_haves:
  truths:
    - "WebhookEvent model stores Stripe event IDs for idempotency checking"
    - "WebhookEvent model tracks event processing status (pending, processing, completed, failed)"
    - "WebhookEvent model stores full event payload for debugging"
    - "WebhookEvent unique index on eventId prevents duplicate processing"
    - "WebhookEvent compound indexes support efficient event queries"
  artifacts:
    - path: "server/types/webhook.ts"
      provides: "TypeScript types for webhook event handling"
      exports: ["IWebhookEvent", "WebhookEventStatus"]
    - path: "server/models/WebhookEvent.ts"
      provides: "Mongoose model for webhook event logging and idempotency"
      contains: "WebhookEventSchema"
      min_lines: 60
  key_links:
    - from: "server/models/WebhookEvent.ts"
      to: "Stripe.Event"
      via: "TypeScript type import"
      pattern: "import.*Event.*from.*stripe"
---

<objective>
Create webhook event logging model and TypeScript types to support idempotency, event tracking, and debugging for Stripe webhook handlers.

Purpose: Webhook events must be logged to prevent duplicate processing (idempotency) and provide debugging visibility. The WebhookEvent model serves as the foundation for reliable webhook handling in Phase 08.

Output: TypeScript types for webhook events and a Mongoose WebhookEvent model with unique eventId constraint, status tracking, and efficient indexes.
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-webhook-infrastructure/08-RESEARCH.md

# Prior Phase 07 provides foundation
@.planning/phases/07-database-api-foundation/07-01-SUMMARY.md
- Subscription model pattern (Mongoose schema with timestamps, indexes, TypeScript interface)
- Payment model pattern for status tracking and metadata

</context>

<tasks>

<task type="auto">
  <name>Create webhook TypeScript types</name>
  <files>server/types/webhook.ts</files>
  <action>
    Create server/types/webhook.ts with:

    1. WebhookEventStatus enum: 'pending' | 'processing' | 'completed' | 'failed'
    2. IWebhookEvent interface extending Document with:
       - eventId: string (Stripe event ID, unique)
       - type: string (Event type like 'checkout.session.completed')
       - status: WebhookEventStatus (processing state)
       - data: any (Full event payload for debugging)
       - error?: string (Error message if failed)
       - receivedAt: Date (Timestamp when webhook received)
       - processedAt?: Date (Timestamp when processing completed)
    3. Export types for use in webhook handlers

    Follow the pattern from server/types/subscription.ts (enum + interface pattern).
  </action>
  <verify>File exists at server/types/webhook.ts with WebhookEventStatus enum and IWebhookEvent interface. TypeScript compilation passes.</verify>
  <done>WebhookEventStatus enum and IWebhookEvent interface are defined and exported for use in WebhookEvent model.</done>
</task>

<task type="auto">
  <name>Create WebhookEvent Mongoose model</name>
  <files>server/models/WebhookEvent.ts</files>
  <action>
    Create server/models/WebhookEvent.ts with:

    1. Import IWebhookEvent and WebhookEventStatus from server/types/webhook.ts
    2. Define WebhookEventSchema with:
       - eventId (String, required, unique, indexed) - Unique Stripe event ID for idempotency
       - type (String, required, indexed) - Event type for filtering and analysis
       - status (enum: 'pending'|'processing'|'completed'|'failed', default: 'pending', indexed)
       - data (Schema.Types.Mixed, required) - Full event payload for debugging
       - error (String, optional) - Error message if failed
       - receivedAt (Date, required, indexed) - Timestamp when webhook received
       - processedAt (Date, optional) - Timestamp when processing completed
       - timestamps: true (auto-manage createdAt/updatedAt)
    3. Compound index: { type: 1, receivedAt: -1 } for querying recent events by type
    4. TTL index on receivedAt with 30-day expiration (expireAfterSeconds: 2592000)
    5. Export WebhookEvent model

    Follow the Mongoose schema pattern from server/models/Subscription.ts (indexes, validation, timestamps).
  </action>
  <verify>File exists at server/models/WebhookEvent.ts with WebhookEventSchema, all required fields, indexes, and TTL. Model exports correctly.</verify>
  <done>WebhookEvent model is ready for importing in webhook handlers. Unique eventId constraint prevents duplicate processing.</done>
</task>

<task type="auto">
  <name>Verify WebhookEvent model compiles and indexes are defined</name>
  <files>server/models/WebhookEvent.ts</files>
  <action>
    1. Run TypeScript type check: bun typecheck
    2. Verify model compiles without errors
    3. Confirm indexes are defined by inspecting schema:
       - Unique index on eventId
       - Single index on type
       - Single index on status
       - Single index on receivedAt
       - Compound index on { type, receivedAt }
       - TTL index on receivedAt (30 days)
    4. Log index creation summary

    This is a verification task - no code changes expected.
  </action>
  <verify>TypeScript typecheck passes. Indexes are correctly defined in WebhookEventSchema.</verify>
  <done>WebhookEvent model is properly typed with all required indexes for idempotency and efficient querying.</done>
</task>

</tasks>

<verification>
## Phase Checks

After completing all tasks, verify:

1. **TypeScript types defined**: server/types/webhook.ts exists with WebhookEventStatus enum and IWebhookEvent interface
2. **Model created**: server/models/WebhookEvent.ts exists with WebhookEventSchema
3. **Unique constraint**: eventId field has unique index to prevent duplicate event processing
4. **Indexes defined**: 6 indexes (eventId, type, status, receivedAt, compound, TTL) for query performance
5. **Type safety**: TypeScript compilation passes without errors

## Integration Verification

WebhookEvent model should be importable:
```typescript
import { WebhookEvent } from '../../models/WebhookEvent'
import { IWebhookEvent, WebhookEventStatus } from '../../types/webhook'
```
</verification>

<success_criteria>
Phase 08 Plan 01 is complete when:
1. WebhookEvent model can track Stripe webhook events with unique eventId constraint
2. Event processing status can be monitored (pending → processing → completed/failed)
3. Full event payloads are stored for debugging failed webhooks
4. TTL index automatically cleans up old events after 30 days
5. TypeScript types provide compile-time safety for webhook handlers
</success_criteria>

<output>
After completion, create `.planning/phases/08-webhook-infrastructure/08-01-SUMMARY.md`
</output>
