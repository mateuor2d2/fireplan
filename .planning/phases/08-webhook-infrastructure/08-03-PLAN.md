---
phase: 08-webhook-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - server/utils/webhookHandlers.ts
  - server/api/webhooks/stripe.post.ts
autonomous: true

must_haves:
  truths:
    - "checkout.session.completed handler updates Payment and Plan models"
    - "payment_intent.payment_failed handler marks payment as failed"
    - "customer.subscription.created handler creates Subscription record and enables QR access"
    - "customer.subscription.updated handler syncs subscription status from Stripe"
    - "customer.subscription.deleted handler cancels subscription and disables QR access"
    - "invoice.payment_succeeded handler extends subscription access period"
    - "invoice.payment_failed handler marks subscription as past_due"
  artifacts:
    - path: "server/utils/webhookHandlers.ts"
      provides: "Stripe webhook event handler functions"
      exports: ["handleCheckoutSessionCompleted", "handlePaymentIntentFailed", "handleSubscriptionCreated", "handleSubscriptionUpdated", "handleSubscriptionDeleted", "handleInvoicePaymentSucceeded", "handleInvoicePaymentFailed"]
      min_lines: 200
  key_links:
    - from: "server/utils/webhookHandlers.ts"
      to: "Payment model"
      via: "import and database update"
      pattern: "Payment\.(findOneAndUpdate|findByIdAndUpdate)"
    - from: "server/utils/webhookHandlers.ts"
      to: "Subscription model"
      via: "import and database operations"
      pattern: "Subscription\.(findOne|create|save)"
    - from: "server/utils/webhookHandlers.ts"
      to: "Planes model"
      via: "import for QR access updates"
      pattern: "Planes\.(findByIdAndUpdate)"
    - from: "server/api/webhooks/stripe.post.ts"
      to: "webhookHandlers"
      via: "import and function calls"
      pattern: "handle(Event|ByType).*from.*webhookHandlers"
---

<objective>
Implement Stripe webhook event handlers for checkout sessions, payments, subscription lifecycle, and invoice events to keep database synchronized with Stripe state.

Purpose: Webhook events are the source of truth for payment and subscription state. These handlers update local database records (Payment, Subscription, Plan) when Stripe events occur, ensuring the application reflects the actual state in Stripe.

Output: Comprehensive webhook handler functions in server/utils/webhookHandlers.ts that process 7 critical Stripe event types and update database records accordingly.
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-webhook-infrastructure/08-RESEARCH.md

# Phase 07 provides models and Stripe utilities
@.planning/phases/07-database-api-foundation/07-01-SUMMARY.md
- Subscription model with stripeSubscriptionId index
- Subscription types (ISubscription)
@.planning/phases/07-database-api-foundation/07-03-SUMMARY.md
- Stripe helper utilities (createQrSubscription, updateQrSubscription, cancelQrSubscription)

# Existing payment model
@server/models/Payment.ts
- Payment model with stripePaymentIntentId index

# Planes model for QR access updates
@server/models/Planes.ts
- Plan model with qrEnabled and subscriptionActive fields
</context>

<tasks>

<task type="auto">
  <name>Create checkout and payment intent handlers</name>
  <files>server/utils/webhookHandlers.ts</files>
  <action>
    Create server/utils/webhookHandlers.ts with checkout and payment handlers:

    1. Import dependencies:
       - Payment from server/models/Payment
       - Planes from server/models/Planes
       - Stripe types from 'stripe'

    2. Implement handleCheckoutSessionCompleted:
       - Extract userId and planId from session.metadata
       - Throw error if metadata missing
       - Find and update Payment record by stripePaymentIntentId:
         * status: 'succeeded'
         * paymentMethod: 'checkout'
         * description: session.description or 'Checkout session payment'
       - Throw error if payment not found
       - Update Planes record by planId:
         * paymentStatus: 'paid'
         * paidAt: new Date()
         * canPrint: true
       - Log success message

       Reference "Checkout Session Completed Handler" from 08-RESEARCH.md.

    3. Implement handlePaymentIntentFailed:
       - Find Payment record by payment_intent id
       - Update payment status to 'failed'
       - Log error with payment intent ID

    4. Export both functions

    Handlers should be async functions with proper error handling.
  </action>
  <verify>File exists with handleCheckoutSessionCompleted and handlePaymentIntentFailed functions. Both import Payment and Planes models. Functions export correctly.</verify>
  <done>Checkout session completion updates payment and plan records. Payment failures are logged and tracked.</done>
</task>

<task type="auto">
  <name>Create subscription lifecycle handlers</name>
  <files>server/utils/webhookHandlers.ts</files>
  <action>
    Add subscription lifecycle handlers to server/utils/webhookHandlers.ts:

    1. Import Subscription from server/models/Subscription

    2. Implement handleSubscriptionCreated:
       - Extract planId and userId from subscription.metadata
       - Check for existing subscription (idempotency)
       - Create Subscription record with:
         * userId, planId
         * stripeSubscriptionId: subscription.id
         * stripeCustomerId: subscription.customer
         * stripePriceId: subscription.items.data[0].price.id
         * status: subscription.status
         * billingInterval: subscription.items.data[0].recurring.interval
         * currentPeriodStart: subscription.current_period_start * 1000
         * currentPeriodEnd: subscription.current_period_end * 1000
         * cancelAtPeriodEnd: subscription.cancel_at_period_end
         * amount: subscription.items.data[0].price.unit_amount
         * currency: subscription.currency
         * metadata: subscription.metadata
       - Update Planes record:
         * qrEnabled: true
         * subscriptionActive: true
       - Log success

       Reference "Subscription Created Handler" from 08-RESEARCH.md.

    3. Implement handleSubscriptionUpdated:
       - Find Subscription by stripeSubscriptionId
       - Throw error if not found
       - Update subscription fields:
         * status: subscription.status
         * currentPeriodStart: subscription.current_period_start * 1000
         * currentPeriodEnd: subscription.current_period_end * 1000
         * cancelAtPeriodEnd: subscription.cancel_at_period_end
       - Handle pause_collection: set status to 'paused' if present
       - Save subscription
       - Log success

       Reference Pattern 4 from 08-RESEARCH.md.

    4. Implement handleSubscriptionDeleted:
       - Find Subscription by stripeSubscriptionId
       - Update status to 'canceled'
       - Update Planes record:
         * subscriptionActive: false
         * subscriptionStatus: 'canceled'
       - Save subscription
       - Log success

    5. Export all three functions
  </action>
  <verify>handleSubscriptionCreated, handleSubscriptionUpdated, and handleSubscriptionDeleted functions exist. All use Subscription model. Functions export correctly.</verify>
  <done>Subscription lifecycle events sync local database with Stripe. Created, updated, and deleted subscriptions are tracked.</done>
</task>

<task type="auto">
  <name>Create invoice event handlers and wire to endpoint</name>
  <files>server/utils/webhookHandlers.ts, server/api/webhooks/stripe.post.ts</files>
  <action>
    Add invoice handlers to server/utils/webhookHandlers.ts:

    1. Implement handleInvoicePaymentSucceeded:
       - Find Subscription by stripeSubscriptionId (invoice.subscription)
       - Throw error if not found
       - Update Planes record by subscription.planId:
         * canPrint: true
         * subscriptionValidUntil: invoice.period_end * 1000
       - Log success

       Reference "Invoice Payment Succeeded Handler" from 08-RESEARCH.md.

    2. Implement handleInvoicePaymentFailed:
       - Find Subscription by stripeSubscriptionId
       - Update subscription status to 'past_due'
       - Update Planes record by subscription.planId:
         * subscriptionStatus: 'past_due'
         * subscriptionValidUntil: new Date() (access ends now)
       - Save subscription
       - Log failure

       Reference "Invoice Payment Failed Handler" from 08-RESEARCH.md.

    3. Export both invoice handlers

    4. Update server/api/webhooks/stripe.post.ts:
       - Import all handlers from server/utils/webhookHandlers
       - Replace console.log stubs in handleEventByType with actual handler calls:
         ```typescript
         case 'checkout.session.completed':
           await handleCheckoutSessionCompleted(stripeEvent.data.object)
           break
         case 'payment_intent.payment_failed':
           await handlePaymentIntentFailed(stripeEvent.data.object)
           break
         case 'customer.subscription.created':
           await handleSubscriptionCreated(stripeEvent.data.object)
           break
         case 'customer.subscription.updated':
           await handleSubscriptionUpdated(stripeEvent.data.object)
           break
         case 'customer.subscription.deleted':
           await handleSubscriptionDeleted(stripeEvent.data.object)
           break
         case 'invoice.payment_succeeded':
           await handleInvoicePaymentSucceeded(stripeEvent.data.object)
           break
         case 'invoice.payment_failed':
           await handleInvoicePaymentFailed(stripeEvent.data.object)
           break
         ```
  </action>
  <verify>handleInvoicePaymentSucceeded and handleInvoicePaymentFailed functions exist. Webhook endpoint imports and calls all handlers. Event router dispatches to correct handlers.</verify>
  <done>Invoice payment events extend or revoke subscription access. All 7 event types are handled with proper database updates.</done>
</task>

</tasks>

<verification>
## Phase Checks

After completing all tasks, verify:

1. **All handlers created**: 7 handler functions in webhookHandlers.ts
2. **Handler functions export**: All functions are properly exported
3. **Database updates**: Each handler updates appropriate model (Payment, Subscription, Planes)
4. **Error handling**: Handlers throw errors for missing records with descriptive messages
5. **Metadata validation**: Handlers validate required metadata fields (userId, planId)
6. **Idempotency**: handleSubscriptionCreated checks for existing subscriptions
7. **Wiring complete**: Webhook endpoint imports and calls all handlers in event router
8. **Logging**: Each handler logs success/failure for debugging

## Integration Verification

Webhook handlers should:
- Import Payment, Subscription, and Planes models
- Accept Stripe event objects as parameters (typed)
- Update database records with findOneAndUpdate or save
- Handle missing records with error logging
- Export for use in webhook endpoint

Webhook endpoint should:
- Import all 7 handlers from webhookHandlers
- Call handlers in handleEventByType switch statement
- Pass event data objects to handlers
</verification>

<success_criteria>
Phase 08 Plan 03 is complete when:
1. checkout.session.completed updates Payment and Planes records
2. payment_intent.payment_failed marks payment as failed
3. customer.subscription.created creates Subscription and enables QR access
4. customer.subscription.updated syncs subscription status with Stripe
5. customer.subscription.deleted cancels subscription and disables QR access
6. invoice.payment_succeeded extends subscription access period
7. invoice.payment_failed marks subscription as past_due and revokes access
8. All handlers are wired to webhook endpoint event router
</success_criteria>

<output>
After completion, create `.planning/phases/08-webhook-infrastructure/08-03-SUMMARY.md`
</output>
