---
phase: 12-testing-monitoring
task: 0
total_tasks: 1
status: ready_to_execute
last_updated: 2026-02-14T16:10:20.449Z
---

<current_state>
Phase 12: Testing & Monitoring - Gap Closure Planning Complete

The gap closure plan (12-06) has been created to fix the invoice history API missing hosted_invoice_url issue. The plan is ready to execute.

**Gap identified from VERIFICATION.md:**
- Invoice history endpoint doesn't include Stripe-hosted invoice URL
- This prevents the payment history UI from showing download buttons

**Gap closure approach:** Backend fix - modify `server/api/invoices/history.get.ts` to call Stripe API and include `invoiceUrl` field in response.
</current_state>

<completed_work>

- Phase 12 Plans 01-05: Complete (invoice download, email notifications, history filters, grace period helper, webhook handler)
- Phase 12 Verification: Complete - identified 1 integration gap
- Phase 12 Gap Closure Planning: Complete - created plan 12-06

**Plans executed:**
- 12-01: Invoice PDF Download Endpoint ✅
- 12-02: Payment Failure Email Notifications ✅
- 12-03: Payment History Filters ✅
- 12-04: Subscription Access Grace Period Helper ✅
- 12-05: Invoice Payment Failure Webhook Handler ✅

**Gap Closure Plan Created:**
- 12-06: Add Stripe invoice URL retrieval to history endpoint (not yet executed)
</completed_work>

<remaining_work>

**Plan 12-06: Add Stripe invoice URL retrieval to history endpoint**
- Task 1: Modify `server/api/invoices/history.get.ts` to include hosted_invoice_url
  - Import stripe utility
  - Call stripe.invoices.retrieve() with expand: ['hosted_invoice_url']
  - Add invoiceUrl field to response object

**After 12-06 execution:**
- Phase 12 will be 100% complete
- v1.1 Stripe Payments milestone will be complete
- Ready for milestone audit and completion
</remaining_work>

<decisions_made>

- **Gap Closure Approach:** Chose backend fix over frontend fix because:
  1. Matches frontend expectation exactly (UI expects `invoiceUrl` field)
  2. Single point of change (no frontend modifications)
  3. Uses proven pattern from download endpoint
  4. N+1 pattern acceptable for paginated invoice history (~10 items)

- **Implementation Pattern:** Will use the same approach as the existing download endpoint (`server/api/invoices/[id]/download.get.ts`) which calls `stripe.invoices.retrieve(id, { expand: ['hosted_invoice_url'] })`
</decisions_made>

<blockers>
None - gap closure plan is straightforward and ready to execute.
</blockers>

<context>
**Session Summary:**
We ran `/gsd:plan-phase 12 --gaps` to close the identified gap from verification. The gsd-planner agent created plan 12-06 which adds the Stripe-hosted invoice URL to the invoice history API response.

**The Gap:**
The invoice history endpoint (`server/api/invoices/history.get.ts`) was implemented during plan 12-03, but it didn't include the Stripe-hosted invoice URL that the UI expects. This prevents download buttons from appearing in the payment history page.

**The Fix:**
Add a Stripe API call to retrieve `hosted_invoice_url` for each invoice in the history response. This is a simple modification that follows the existing pattern from the download endpoint.

**Milestone v1.1 Status:**
- 96% complete (phases 7-11 done, phase 12 plans 01-05 done)
- Only gap closure plan 12-06 remains
- After execution: 100% complete, ready for milestone completion
</context>

<next_action>
Execute gap closure plan: `/gsd:execute-phase 12-testing-monitoring --gaps-only`

This will run plan 12-06 which modifies `server/api/invoices/history.get.ts` to include the Stripe-hosted invoice URL in the API response, enabling download buttons in the payment history UI.
</next_action>
