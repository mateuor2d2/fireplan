---
phase: 12-testing-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/api/invoices/[id]/download.get.ts
  - server/types/invoice.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Users can download invoice PDF by clicking download button"
    - "Invoice download redirects to Stripe-hosted PDF"
    - "Users can only download their own invoices (authorization)"
  artifacts:
    - path: "server/api/invoices/[id]/download.get.ts"
      provides: "Invoice PDF download endpoint"
      exports: ["GET"]
    - path: "server/types/invoice.ts"
      provides: "IInvoice interface with hosted_invoice_url field"
      contains: "hosted_invoice_url?: string"
  key_links:
    - from: "server/api/invoices/[id]/download.get.ts"
      to: "https://api.stripe.com/v1/invoices/:id"
      via: "stripe.invoices.retrieve(invoiceId)"
      pattern: "stripe\.invoices\.retrieve"
    - from: "server/api/payments/history.get.ts"
      to: "server/api/invoices/[id]/download.get.ts"
      via: "Invoice URL in response"
      pattern: "hosted_invoice_url|downloadUrl|pdfUrl"

---

<objective>
Add invoice PDF download endpoint using Stripe-hosted invoice URLs

Purpose: Users need downloadable invoice PDFs for their payments and subscriptions. Stripe automatically hosts invoice PDFs, so we redirect to the hosted URL rather than generating custom PDFs.

Output: Working `/api/invoices/[id]/download` endpoint that redirects to Stripe-hosted PDF
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-testing-monitoring/12-testing-monitoring-RESEARCH.md
@.planning/phases/07-database-api-foundation/07-02-SUMMARY.md
@.planning/phases/08-webhook-infrastructure/08-03-SUMMARY.md

# Reference prior plan for invoice model structure
@.planning/phases/07-database-api-foundation/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add invoice download endpoint</name>
  <files>server/api/invoices/[id]/download.get.ts</files>
  <action>
    Create GET endpoint at `server/api/invoices/[id]/download.get.ts`:

    1. Import dependencies:
       - `stripe`, `sendRedirect` from 'h3'
       - `Invoice` model from '~/server/models/Invoice'
       - `User` from '~/server/models/User' (already imported by auth middleware)

    2. Get authenticated user from event.context.user

    3. Get invoiceId from route params: `getRouterParam(event, 'id')`

    4. Query invoice from database:
       - Find by `_id: invoiceId, userId: user._id`
       - Throw 404 if not found (invoice not found)
       - This ensures users can only download their own invoices

    5. Retrieve Stripe invoice with hosted URL:
       - Call `stripe.invoices.retrieve(invoice.stripeInvoiceId, { expand: ['hosted_invoice_url'] })`
       - This fetches the hosted PDF URL from Stripe

    6. Validate hosted_invoice_url exists:
       - Throw 400 if missing (Invoice PDF not available)

    7. Redirect to Stripe-hosted PDF:
       - Return `sendRedirect(event, stripeInvoice.hosted_invoice_url)`
       - Browser redirects to Stripe's hosted PDF URL

    8. Add error handling with try/catch
  </action>
  <verify>
    1. File exists at `server/api/invoices/[id]/download.get.ts`
    2. Endpoint retrieves invoice with userId ownership check
    3. Stripe invoice retrieved with expand: ['hosted_invoice_url']
    4. Returns redirect to Stripe-hosted URL
    5. TypeScript compiles without errors
  </verify>
  <done>
    Endpoint returns 302 redirect to Stripe-hosted invoice PDF URL
    Users can only download their own invoices (userId check)
    Missing hosted_invoice_url returns 400
  </done>
</task>

<task type="auto">
  <name>Add hosted_invoice_url to Invoice interface</name>
  <files>server/types/invoice.ts</files>
  <action>
    Add optional `hosted_invoice_url?: string` field to `IInvoice` interface in `server/types/invoice.ts`

    This field is populated when retrieving invoices from Stripe with expand option.
  </action>
  <verify>
    1. Interface has `hosted_invoice_url?: string` field
    2. Field is marked optional (?:)
    3. TypeScript compiles without errors
  </verify>
  <done>
    IInvoice interface includes hosted_invoice_url field for type safety
  </done>
</task>

</tasks>

<verification>
1. Created endpoint at `server/api/invoices/[id]/download.get.ts`
2. Endpoint validates user owns invoice (userId check)
3. Endpoint retrieves Stripe invoice with hosted_invoice_url
4. Endpoint redirects to Stripe-hosted PDF URL
5. IInvoice interface updated with hosted_invoice_url field
</verification>

<success_criteria>
1. Users can download invoice PDFs via `/api/invoices/[id]/download`
2. Download endpoint redirects to Stripe-hosted PDF URL
3. Users can only download their own invoices (authorization check)
4. Missing hosted_invoice_url returns appropriate error
</success_criteria>

<output>
After completion, create `.planning/phases/12-testing-monitoring/12-01-SUMMARY.md`
</output>
