---
phase: 12-testing-monitoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/utils/email.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Payment failure emails are sent via Mailgun"
    - "Email template includes plan name, invoice URL, and retry info"
    - "Email function is reusable for different payment failure scenarios"
  artifacts:
    - path: "server/utils/email.ts"
      provides: "sendPaymentFailureEmail utility function"
      exports: ["sendPaymentFailureEmail"]
      min_lines: 40
  key_links:
    - from: "server/api/payments/webhook.post.ts"
      to: "server/utils/email.ts"
      via: "sendPaymentFailureEmail call"
      pattern: "sendPaymentFailureEmail\\("
    - from: "server/utils/email.ts"
      to: "https://api.mailgun.net/v3/messages"
      via: "Mailgun SDK"
      pattern: "mg\\.messages\\.create"

---

<objective>
Add payment failure email notification utility using Mailgun

Purpose: When subscription payments fail, users need email notifications so they can update payment methods. Use existing Mailgun infrastructure with Stripe-hosted invoice URLs for easy payment updates.

Output: Reusable `sendPaymentFailureEmail` function in email.ts utilities
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-testing-monitoring/12-testing-monitoring-RESEARCH.md

# Reference existing email utilities pattern
@.planning/phases/02-user-profile-settings/02-04-SUMMARY.md
# Reference Mailgun configuration
@.planning/phases/08-webhook-infrastructure/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add payment failure email function</name>
  <files>server/utils/email.ts</files>
  <action>
    Add `sendPaymentFailureEmail` function to `server/utils/email.ts`:

    ```typescript
    export async function sendPaymentFailureEmail(
      email: string,
      planName: string,
      invoiceUrl: string,
      retryCount: number
    ): Promise<void>
    ```

    Implementation:

    1. Get runtime config with `useRuntimeConfig()`

    2. Create Mailgun client (same as sendResetEmail):
       ```typescript
       const mailgun = new Mailgun(FormData)
       const mg = mailgun.client({
         username: 'api',
         key: config.mailgun.apiKey
       })
       ```

    3. Construct email data:
       - from: `${config.mailgun.fromName} <postmaster@${config.mailgun.domain}>`
       - to: email (user email)
       - subject: 'Acción Requerida: Pago Fallido' (Action Required: Payment Failed)

    4. Build HTML email template:
       ```html
       <div style="font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto;">
         <h2 style="color: #dc2626;">Pago Fallido para ${planName}</h2>
         <p>Hemos tenido un problema al procesar tu pago.</p>
         <p><strong>Detalles del problema:</strong> El pago no ha podido ser procesado.</p>
         <p>Por favor, actualiza tu método de pago para continuar con tu suscripción.</p>
         <p style="text-align: center; margin: 30px 0;">
           <a href="${invoiceUrl}" style="
             display: inline-block;
             padding: 12px 24px;
             background-color: #10b981;
             color: white;
             text-decoration: none;
             border-radius: 6px;
             font-weight: bold;
           ">
             Ver Factura y Actualizar Pago
           </a>
         </p>
         <p style="background-color: #f3f4f6; padding: 15px; border-radius: 4px; border-left: 4px solid #ffa500;">
           <strong>ℹ️ Información Importante:</strong><br>
           Tu suscripción se mantendrá activa durante 7 días mientras Stripe reintentar el pago automáticamente.<br>
           No perderás acceso al servicio durante este periodo.
         </p>
         <p style="font-size: 12px; color: #666;">
           Intentos de reintentos restantes: ${retryCount}<br>
           Si el problema persiste, contacta con soporte técnico.
         </p>
       </div>
       ```

    5. Send email via Mailgun:
       ```typescript
       await mg.messages.create(config.mailgun.domain, data)
       ```

    6. Add error handling with try/catch
    7. Add console.log for successful sending

    Key features:
    - Spanish language (matches app locale)
    - Clear call-to-action button with invoice URL
    - Grace period notice (7 days during retries)
    - Retry count display
    - Professional styling with inline CSS
  </action>
  <verify>
    1. Function `sendPaymentFailureEmail` exists in email.ts
    2. Function accepts 4 parameters (email, planName, invoiceUrl, retryCount)
    3. Uses Mailgun client (same pattern as sendResetEmail)
    4. Email template includes plan name, invoice URL, and retry info
    5. Email is in Spanish
    6. Function includes error handling
    </verify>
  <done>
    sendPaymentFailureEmail function created in server/utils/email.ts
    Email template includes invoice URL button for easy payment update
    Grace period notice informs users access continues during retries
  </done>
</task>

</tasks>

<verification>
1. Created `sendPaymentFailureEmail` function in server/utils/email.ts
2. Function uses existing Mailgun infrastructure
3. Email template includes invoice URL, plan name, and retry count
4. Email informs users of 7-day grace period
5. Function is async with proper error handling
</verification>

<success_criteria>
1. Payment failure email function exists and is reusable
2. Email template includes actionable invoice URL button
3. Email informs users of grace period (7 days)
4. Function follows same pattern as existing sendResetEmail
</success_criteria>

<output>
After completion, create `.planning/phases/12-testing-monitoring/12-02-SUMMARY.md`
</output>
