---
phase: 12-testing-monitoring
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - server/api/invoices/history.get.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Invoice history API includes hosted_invoice_url for download feature"
    - "Payment history UI shows invoice download button"
  artifacts:
    - path: "server/api/invoices/history.get.ts"
      provides: "Invoice history with Stripe hosted_invoice_url"
      min_lines: 110
      contains: "stripe.invoices.retrieve"
    - path: "app/pages/protected/usuarios/payments/index.vue"
      provides: "Payment history UI with download buttons"
      min_lines: 275
  key_links:
    - from: "server/api/invoices/history.get.ts"
      to: "Stripe API"
      via: "stripe.invoices.retrieve with expand: ['hosted_invoice_url']"
      pattern: "stripe\\.invoices\\.retrieve.*expand.*hosted_invoice_url"
    - from: "app/pages/protected/usuarios/payments/index.vue"
      to: "server/api/invoices/history.get.ts"
      via: "useLazyAsyncData fetch"
      pattern: "\\$fetch.*api/invoices/history"
---

<objective>
Add Stripe-hosted invoice URLs to invoice history API response to enable download buttons in payment history UI.

Purpose: Close integration gap where invoice history endpoint doesn't include hosted_invoice_url, preventing UI from showing download buttons
Output: Working invoice download buttons in payment history page
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-testing-monitoring/12-testing-monitoring-VERIFICATION.md
@.planning/phases/12-testing-monitoring/12-03-SUMMARY.md
@.planning/phases/12-testing-monitoring/12-01-SUMMARY.md

# Reference implementation (same pattern used in download endpoint)
@server/api/invoices/[id]/download.get.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stripe invoice URL retrieval to history endpoint</name>
  <files>server/api/invoices/history.get.ts</files>
  <action>
    Modify server/api/invoices/history.get.ts to include hosted_invoice_url for each invoice:

    1. Import stripe utility (add to existing imports):
       import { stripe } from '~/server/utils/stripe'

    2. In the invoicesWithPlans Promise.all map (lines 62-76), add Stripe API call to retrieve hosted_invoice_url for each invoice:

    const stripeInvoice = await stripe.invoices.retrieve(
      invoice.stripeInvoiceId,
      { expand: ['hosted_invoice_url'] }
    )

    3. Add invoiceUrl field to the returned object:
    Return object should include:
    - All existing invoice fields (...invoice)
    - plan (existing)
    - invoiceUrl: stripeInvoice.hosted_invoice_url (new field)

    Pattern matches download endpoint (server/api/invoices/[id]/download.get.ts:44-47).

    DO NOT modify pagination logic or filter logic - only add the invoiceUrl field.
  </action>
  <verify>
    After implementation, verify by:
    1. Check server/api/invoices/history.get.ts includes stripe import
    2. Check that stripe.invoices.retrieve is called with expand: ['hosted_invoice_url']
    3. Check that returned object includes invoiceUrl field
    4. No other logic changes (pagination, filters unchanged)
  </verify>
  <done>
    Invoice history API response includes invoiceUrl field with Stripe-hosted invoice PDF URL for each invoice
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
1. API returns invoiceUrl field for each invoice in history
2. Frontend shows download button for invoices (expects invoiceUrl field)
3. Download button navigates to Stripe-hosted invoice PDF
4. No performance regression (pagination still works, filters unchanged)
5. Error handling for invoices without hosted_invoice_url (graceful degradation)
</verification>

<success_criteria>
1. Invoice history API response includes invoiceUrl field (string) for each invoice
2. Payment history UI displays download button for each invoice
3. Clicking download button opens Stripe-hosted invoice PDF
4. Existing functionality (filters, pagination) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/12-testing-monitoring/12-06-SUMMARY.md`
</output>
