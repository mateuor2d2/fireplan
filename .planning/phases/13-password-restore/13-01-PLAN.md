---
phase: 13-password-restore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/forgot-password.vue
  - app/pages/login.vue
autonomous: false
user_setup: []

must_haves:
  truths:
    - "User can navigate to /forgot-password page"
    - "User sees email input form with clear instructions"
    - "User receives success state after submitting email"
    - "User can navigate back to login from success state"
  artifacts:
    - path: "app/pages/forgot-password.vue"
      provides: "Dedicated forgot password page with email form and success state"
      min_lines: 100
    - path: "app/pages/login.vue"
      provides: "Login page with updated 'Forgot password?' link pointing to new page"
      contains: "ULink to='/forgot-password'"
  key_links:
    - from: "app/pages/forgot-password.vue"
      to: "/api/auth/forgot-password"
      via: "userStore.forgotPassword(email) call"
      pattern: "forgotPassword\\(.*email"
    - from: "app/pages/login.vue"
      to: "app/pages/forgot-password.vue"
      via: "ULink component with to='/forgot-password' prop"
      pattern: "to=.*forgot-password"
---

<objective>
Create dedicated forgot-password page with email form and success state, improving UX over inline reset flow.

Purpose: Users who have forgotten their password need a clear, dedicated page to request a reset link. Current implementation uses inline "Forgot password?" on login page which provides minimal feedback. A dedicated page provides better user experience with clear instructions and success confirmation.

Output: New `/forgot-password` page accessible from login page, with email form submission and success state showing "Check your email" message.
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-password-restore/13-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing infrastructure (already implemented - DO NOT modify)
- server/api/auth/forgot-password.post.ts (POST endpoint for reset requests)
- server/api/auth/reset-password.post.ts (POST endpoint for password reset)
- server/models/PasswordResetToken.ts (reset token model with 1h expiration)
- app/pages/reset-password.vue (reset form with password strength indicator)
- app/stores/user.ts (forgotPassword() method at lines 283-300)
- server/utils/email.ts (sendResetEmail() with HTML template)
- app/pages/login.vue (has existing "Forgot password?" inline link)

# Pattern references
- app/pages/login.vue (auth layout pattern, UAuthForm component usage)
- app/pages/reset-password.vue (UCard layout pattern, loading states, Spanish messages)
- app/pages/protected/settings/perfil.vue (UAlert, UForm, UButton patterns)
</context>

<tasks>

<task type="auto">
  <name>Create forgot-password.vue page</name>
  <files>app/pages/forgot-password.vue</files>
  <action>
    Create new forgot-password page at `app/pages/forgot-password.vue`:

    1. Page structure:
       - Use `definePageMeta({ layout: 'auth' })` following reset-password.vue pattern
       - Set SEO meta: title "Restablecer Contraseña" (Spanish for "Reset Password")
       - Use UCard component with max-w-sm w-full sizing pattern

    2. Component state (using refs):
       - email: ref('')
       - isLoading: ref(false)
       - isSubmitted: ref(false)
       - error: ref('')

    3. Form elements (UCard template):
       - Icon: i-heroicons-envelope-key (lock/key icon for password reset)
       - Header: "Restablecer Contraseña"
       - Description: "Introduce tu correo electrónico y te enviaremos un enlace para restablecer tu contraseña"
       - UInput for email with type="email", required, placeholder="tu@email.com"
       - UButton: "Enviar enlace de restablecimiento" (primary, block, :loading="isLoading")
       - Error message display (red-500, text-center, v-if="error")

    4. Success state (replace form when isSubmitted=true):
       - Icon: i-heroicons-check-circle (emerald-500)
       - Header: "Correo enviado"
       - Description: "Hemos enviado un enlace a tu correo electrónico. Sigue las instrucciones para restablecer tu contraseña."
       - Note: "Si no recibes el correo en unos minutos, verifica tu carpeta de spam."
       - UButton: "Volver al inicio" (to="/login", variant="soft", color="primary")

    5. Form submission handler:
       - Validate email is not empty
       - Call userStore.forgotPassword(email.value)
       - Set isLoading during request
       - On success: set isSubmitted=true
       - On error: set error with error.message || 'Error al enviar correo'

    6. Follow existing patterns:
       - Use useToast() for error notifications (like login.vue)
       - Email validation regex: /^[^\s@]+@[^\s@]+\.[^\s@]+$/
       - Spanish labels throughout
       - Nuxt UI v4 styling (UCard, UInput, UButton, UIcon)
       - Backdrop blur effect: class="bg-white/75 dark:bg-white/5 backdrop-blur"

    Minimum 100 lines. Follow exact styling patterns from reset-password.vue for consistency.
  </action>
  <verify>
    File exists at app/pages/forgot-password.vue with min_lines >= 100
  </verify>
  <done>
    /forgot-password route displays email form with Spanish labels, success state after submission, and link back to login
  </done>
</task>

<task type="auto">
  <name>Update login.vue Forgot password link</name>
  <files>app/pages/login.vue</files>
  <action>
    Update the "Forgot password?" link in login.vue to point to new /forgot-password page:

    1. Locate existing "Forgot password?" link (lines 138-148 in current file)
    2. Replace inline UButton with ULink component for navigation:
       - Change from: `<UButton variant="link" @click="handleForgotPassword">`
       - Change to: `<ULink to="/forgot-password" class="text-primary font-medium">`
    3. Remove handleForgotPassword() function (lines 91-119) - no longer needed with dedicated page
    4. Update text to Spanish: "¿Olvidaste tu contraseña?" (more natural than "Forgot password?")
    5. Keep existing template slot: #password-hint

    Before (current):
    ```vue
    <template #password-hint>
      <UButton variant="link" @click="handleForgotPassword">Forgot password?</UButton>
    </template>
    ```

    After (new):
    ```vue
    <template #password-hint>
      ¿Olvidaste tu contraseña?
      <ULink to="/forgot-password" class="text-primary font-medium">Restablecer contraseña</ULink>
    </template>
    ```

    6. Clean up: Remove handleForgotPassword function entirely (lines 91-119) as it's no longer used

    This change improves UX by providing clear navigation to dedicated page instead of inline action.
  </action>
  <verify>
    - app/pages/login.vue contains ULink to="/forgot-password"
    - handleForgotPassword() function removed from file
    - Text shows "¿Olvidaste tu contraseña?" with "Restablecer contraseña" link
  </verify>
  <done>
    Login page "Forgot password?" link navigates to /forgot-password page instead of inline action
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete forgot-password flow:
    1. Created /forgot-password page with email form
    2. Updated login.vue to link to new page (removed inline handler)
    3. Page uses auth layout with UCard, Spanish labels, success state
  </what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000/login
    2. Verify "¿Olvidaste tu contraseña?" text with "Restablecer contraseña" link
    3. Click the link → should navigate to /forgot-password
    4. On forgot-password page:
       - Verify email input form displays with Spanish labels
       - Verify description text explains password reset flow
       - Enter test email and submit → verify loading state
       - Verify success state shows "Correo enviado" message
       - Verify "Volver al inicio" button navigates to /login
    5. Test with empty email → verify client-side validation prevents submission
    6. Verify page styling matches reset-password.vue pattern (UCard, backdrop, icons)
  </how-to-verify>
  <resume-signal>Type "approved" if forgot-password page works correctly, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Overall phase verification:
- [ ] /forgot-password route is accessible and renders correctly
- [ ] Email form submits to userStore.forgotPassword() API
- [ ] Success state displays "Check your email" message with Spanish text
- [ ] Login page link navigates to forgot-password (not inline action)
- [ ] All UI follows Nuxt UI v4 patterns (UCard, UButton, UIcon)
- [ ] Page uses auth layout with backdrop blur styling
- [ ] Spanish localization throughout all labels and messages
</verification>

<success_criteria>
1. User can access dedicated /forgot-password page from login
2. Email form submission triggers password reset API call
3. Success state confirms email was sent with clear next steps
4. User can return to login from both success and initial states
5. Implementation follows existing Nuxt UI v4 patterns and Spanish localization
</success_criteria>

<output>
After completion, create `.planning/phases/13-password-restore/13-01-SUMMARY.md` with:
- Files created/modified
- Spanish labels used
- Implementation notes (pattern references)
- Verification results
</output>
