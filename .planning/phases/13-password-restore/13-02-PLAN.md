---
phase: 13-password-restore
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/pages/protected/settings/perfil.vue
autonomous: false
user_setup: []

must_haves:
  truths:
    - "User can see 'Forgot password?' option in Perfil settings tab"
    - "Clicking option navigates to /forgot-password page"
    - "Link is visually distinct and accessible within profile form"
  artifacts:
    - path: "app/pages/protected/settings/perfil.vue"
      provides: "User profile page with forgot password link in Perfil tab"
      contains: "ULink to='/forgot-password'"
  key_links:
    - from: "app/pages/protected/settings/perfil.vue"
      to: "app/pages/forgot-password.vue"
      via: "ULink navigation component"
      pattern: "to=.*forgot-password"
---

<objective>
Add "Forgot password?" link to Perfil settings tab as alternative entry point for password reset.

Purpose: Users who are logged in but want to change/reset their password may not remember their current password. The Perfil settings tab is a natural place to offer password reset as an alternative to the "Contraseña" tab which requires current password verification.

Output: Perfil settings page includes "Forgot password?" link that navigates to dedicated /forgot-password page.
</objective>

<execution_context>
@/home/mateu/.claude/get-shit-done/workflows/execute-plan.md
@/home/mateu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-password-restore/13-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-profile-settings/02-01-PLAN.md

# Existing infrastructure
- app/pages/forgot-password.vue (created in 13-01)
- app/pages/protected/settings/perfil.vue (user profile form with 9 fields)
- app/pages/protected/settings/contrasena.vue (password change with current verification)

# Pattern references from perfil.vue
- UAlert for info messages (lines 42-50)
- UCard for form containers (lines 52-59)
- UButton with icon, variant="outline" for actions
- Spanish labels: "Información Personal", "Nombre completo", etc.
</context>

<tasks>

<task type="auto">
  <name>Add forgot password link to Perfil tab</name>
  <files>app/pages/protected/settings/perfil.vue</files>
  <action>
    Add "Forgot password?" link to perfil.vue in the Perfil settings tab:

    1. Location: Add after the email field (after line 99, before next form field or before form submit)

    2. Link design using UButton (variant="link", color="primary", icon):
       - Text: "¿Olvidaste tu contraseña?" (Spanish)
       - Icon: i-heroicons-question-mark-circle (info icon)
       - Navigation: ULink to="/forgot-password" (not click handler)

    3. Implementation pattern:
       ```vue
       <!-- After email field (around line 99) -->
       <div class="mt-2">
         <ULink
           to="/forgot-password"
           class="text-primary font-medium text-sm flex items-center gap-1"
         >
           <UIcon name="i-heroicons-question-mark-circle" class="w-4 h-4" />
           ¿Olvidaste tu contraseña?
         </ULink>
       </div>
       ```

    4. Alternative location (better UX): Add as link below email field, not as separate section:
       - Place right after the email input and its "Tu correo electrónico de inicio de sesión" helper text
       - Use small text size (text-xs) to not distract from main form

    5. Style consistency:
       - Use existing Nuxt UI v4 patterns (text-primary for links)
       - Match spacing patterns in the form (mt-2 for margin)
       - Keep subtle (not a primary action, just a helpful link)

    6. Do NOT modify:
       - Profile form structure or validation
       - Save button or form submission logic
       - Any other fields (name, phone, company, etc.)

    This provides users an alternative path to password reset when viewing their profile.
  </action>
  <verify>
    - app/pages/protected/settings/perfil.vue contains ULink to="/forgot-password"
    - Link appears after email field with subtle styling
    - Link text is "¿Olvidaste tu contraseña?" in Spanish
  </verify>
  <done>
    Perfil tab includes "Forgot password?" link that navigates to /forgot-password page
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Password reset access from Perfil settings:
    1. Added "¿Olvidaste tu contraseña?" link to perfil.vue
    2. Link positioned after email field with subtle styling
    3. Link navigates to /forgot-password page (created in 13-01)
  </what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000/protected/settings
    2. Click "Perfil" tab (if not already active)
    3. Scroll to email field section
    4. Verify "¿Olvidaste tu contraseña?" link appears below email input
    5. Click the link → should navigate to /forgot-password page
    6. Verify link styling is subtle (text-primary, small text, doesn't distract from main form)
    7. Verify icon (question mark) displays correctly
    8. Navigate back to settings → verify link is still there and functional
  </how-to-verify>
  <resume-signal>Type "approved" if forgot password link works in Perfil tab, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Overall phase verification:
- [ ] Perfil tab displays "Forgot password?" link
- [ ] Link navigates to /forgot-password page (13-01 must be complete first)
- [ ] Link is positioned after email field for logical placement
- [ ] Link uses Spanish text "¿Olvidaste tu contraseña?"
- [ ] Link styling is subtle (text-sm, doesn't distract from main form)
- [ ] Icon displays correctly (question mark circle)
</verification>

<success_criteria>
1. Users can access password reset from Perfil settings tab
2. Link is discoverable but doesn't distract from main profile form
3. Navigation correctly routes to dedicated /forgot-password page
4. Implementation follows Nuxt UI v4 patterns (ULink, UIcon)
</success_criteria>

<output>
After completion, create `.planning/phases/13-password-restore/13-02-SUMMARY.md` with:
- Files modified (perfil.vue)
- Link placement and styling decisions
- Verification results
</output>
