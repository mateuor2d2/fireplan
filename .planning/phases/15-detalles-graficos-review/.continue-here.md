---
phase: 15-detalles-graficos-review
task: QR Code URL Fixes & UI Improvements
total_tasks: 3
status: in_progress
last_updated: 2026-03-17T21:05:57.629Z
---

<current_state>
Working on QR code generation fixes in the dashboard. The main issue is that QR code URLs are not properly including the plan ID, showing generic localhost URLs instead of the full deterministic URL. Added publicUrl field to database schema and updated services to store it, but regenerate is still not working correctly.
</current_state>

<completed_work>

- Database Schema: Added publicUrl field to PlanQRSchema in server/models/Planes.ts
- Type Definitions: Added publicUrl to PlanQRDocument interface in server/types/qr.ts and IPlanQR in server/types/planes.ts
- QR Service: Updated qrService.ts generateForPlan() and regenerateForPlan() to generate and store publicUrl
- Issue QR Service: Updated issueQRService.ts to store publicUrl for issue reporting QR codes
- UI Improvements: Made QR Preview URLs clickable links in QRPreview.vue
- UI Improvements: Added "Abrir URL" button to QRPreview component action buttons
- UI Improvements: Updated ElementBase.vue icons (dashboard: squares-2x2, issues: flag)
- Error Handling: Fixed subscription API to not log 404 errors (expected when no subscription)
- Debugging: Added console logging to trace QR code generation and public plan access
</completed_work>

<remaining_work>

- Fix QR regenerate still showing URLs without plan ID (current blocker)
- Verify stored publicUrl is correctly retrieved in dashboard
- Test the complete QR code flow end-to-end
- Run linter and typecheck to ensure no errors
</remaining_work>

<decisions_made>

- Store publicUrl in database to avoid URL reconstruction issues on client
- Generate publicUrl server-side with proper baseUrl from environment
- Make URLs clickable in UI for better user experience
- Suppress expected 404 errors for subscription checks (normal state, not error)
- Keep slug-based URL structure: /public/planes/{planId}/{slug}
</decisions_made>

<blockers>

- QR Regenerate Issue: After clicking "Regenerar" in dashboard, the URL still shows without plan ID
  - Added debugging logs to trace: regenerate-qr.post.ts, qrService.ts, planes.get.ts
  - Need to check browser console and server logs to see where planId is being lost
  - Possible issue: planId format or the way it's being passed to URL generation
</blockers>

<context>
This is maintenance/fix work on the QR code system that was implemented in earlier phases. The QR codes have two types:
1. Regular QR (plan access) - goes to /public/planes/{planId}/{slug}
2. Issue QR (reporting) - goes to /public/issues/{slug}/{accessToken}

The issue is specifically with the regular QR codes not storing/retrieving the full URL with plan ID. The database schema now has the publicUrl field, services are updated to populate it, but something is still not working in the flow.

Test plan with ID b38f39a2 has been used for testing. The URL should be: http://localhost:5000/public/planes/b38f39a2/{slug}
</context>

<next_action>
Start with: Check browser console and server logs after regenerating a QR code to see the debug output. Look for logs labeled:
- [QR REGENERATE API] 
- [QR REGENERATE SERVICE]
- [PLANES GET]

Then verify if planId is present in the generated publicUrl.
</next_action>

<files_modified>
app/components/ElementBase.vue
app/components/qr/QRPreview.vue
server/api/planes.get.ts
server/api/planes/[id]/regenerate-qr.post.ts
server/api/planes/[id]/subscription.get.ts
server/api/public/planes/[id]/[slug].get.ts
server/models/Planes.ts
server/services/issueQRService.ts
server/services/qrService.ts
server/types/planes.ts
server/types/qr.ts
</files_modified>
